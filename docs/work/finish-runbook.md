# zclassic23 — FINISH RUNBOOK (from finish-zclassic23 workflow wf_6f3b3ba4-b16, 2026-05-29)

**Binding decision (2026-05-29):** Trust model for the fast import = **Fresh checkpoint + FATAL verify** (option a).
Mint a signed SHA3 UTXO commitment near the import height; import-time verification is FATAL — refuse any
unverifiable above-checkpoint UTXO set. Do NOT ship the connect_tip whole-table-SHA3 broadening (false-fails on reorgs).

> ONE dependency-ordered FINISH RUNBOOK for zclassic23, synthesized from 6 dimension audits and ~30 design verdicts at HEAD 266758f0f (working tree dirty: uncommitted --importblockindex + b8-plan edits). The throughline: the new trust-import fast-cold-sync must be made SAFE and FINISHED before it can be the foundation for everything else, because (a) it is the path that establishes the live tip in seconds, and (b) it currently installs an UNVERIFIED UTXO set as authoritative money — the single largest blocker on the board. The hard ordering is: harden + de-risk the import (security blocker first, then idempotency/shell/test) -> make seconds-to-tip real (batch the per-block fsync, import tail bodies, debounce catchup, guard boot walks) -> fix the MCP 401 so the cutover preflight is observable AT ALL -> seed the event-log projections (UTXO + header_admit) and drive conservation so the 8 preflight gates can converge -> tighten the soak canary -> reach GREEN preflight (safe-auto) -> THE LIVE FLIP (human-gated, never automatic) -> soak -> B8 deletion of the comparison apparatus (which closes most of D5/E6/E7) -> the independent beauty/enforcement work that does NOT depend on the cutover (E1 config scope, E6 baseline split, doc reconciliation, E7-hard) runs in parallel from the start. Several gaps whose verdict was 'revise' are folded into their step with the specific correctness fix that must land first (noted in rationale). Real blockers that gate the flip are in open_questions. The two human gates are the live-chain authority flip and the trust-model/commitment-ceremony decision; both require explicit operator go.

## Ordered steps

### [1] Fix MCP 401 so preflight/state are observable; make the import datadir self-evident  — `safe-auto` (no deps)
items: mcp-rpc-401-blocks-live-verification
Verdict ACCEPT. Nothing in the cutover can be VERIFIED while every zcl_* tool returns -32603/401. Root cause is config, not code: the zcl23 MCP server is registered with a relative -datadir (test-tmp/clean5) whose .cookie does not exist, so read_cookie() fails before contacting the live node. Immediate unblock (operator, out-of-repo): remove the stale 'Pending approval' zcl23 registration, then `claude mcp add zcl23 -- /abs/zclassic23 -mcp -datadir=$HOME/.zclassic-c23 -rpcport=18232`, restart Claude Code. Code change (preventive, security-neutral): in main.c -mcp block resolve datadir via realpath(), add <limits.h>, emit ONE stderr line (resolved datadir, cookie path, readable y/n) — stderr only, never stdout (MCP frames JSON-RPC on stdout). Fold-in: the live symptom is now -32603 not 401 (401 is a stale pre-cookie-gate artifact); state that in the commit. First because every later verification step (preflight gates, soak) needs this surface live.

### [2] Block-index import: trust-bind to checkpoint + PoW spot-check (SECURITY BLOCKER)  — `human-gated` (no deps)
items: trust-utxo-import-unverified-above-checkpoint, trust-imported-headers-no-pow-recheck, import-no-source-authenticity-binding, verify-later-never-triggers
BLOCKER, human-gated (consensus/money trust boundary). The import installs zclassicd's UTXO set (~3128361) as the authoritative tip with ZERO crypto check because the only SHA3 commitment is at 3056758 (below the import height), so the 'will verify later' branch accepts silently and connect_tip's nHeight==cp->height gate never fires. A tampered ~/.zclassic chainstate becomes spendable money. REVISE folds (must change before landing): (a) trust-utxo-import — the spend authority is the live `utxos` table, NOT the tip cursor, so withholding commit_tip does NOT prevent spending forged coins; the real fix is option (b): mint a fresh SHA3 commitment at/near the import height and make the import-time check FATAL on no-matching-commitment, OR refuse/quarantine an above-checkpoint UTXO set that has no enforced commitment. DROP the connect_tip 'crossed-or-landed' broadening (the SHA3 is whole-table-current, not height-snapshotted; it would spuriously FATAL on reorgs). (b) trust-imported-headers — add per-header CheckProofOfWork + check_equihash_solution at IMPORT time (one-time), fanned across a small pthread pool; REUSE header_from_disk_block_index() (has the nSolutionSize overflow guard) instead of a hand-rolled mapper; add chain_params_select(CHAIN_MAIN) on the CLI path; abort-all on any failure. (c) import-no-source-authenticity — bind the header chain to the compiled checkpoint hash@height (fix the `a->header_only==a->header_only` tautology) AND extend binding to the UTXO authority path. (d) verify-later — record the obligation as an observable WARN (non-repaging) blocker, sequenced AFTER (a) lands so it has a real commitment to compare against; do not ship a permanent unactionable CRITICAL page. This is the foundation: the import is how the node reaches tip in seconds, so its trust must be sound before we optimize or flip on top of it. Human go required.

### [3] Block-index import: idempotency flag + C-native LOCK-safe copy (no shell)  — `human-gated` (deps: 2)
items: no-idempotency-flag-destructive-reimport, system-cp-shell-and-failure-handling, system-shell-quote-escaping, shared-path-nfile-negative-reject, have-data-remarked-by-boot-disk-repair
Production-harden the import primitive. no-idempotency (REVISE): mirror leveldb_utxo_migrated with a block_index_imported flag + --force; CRITICAL fold — also short-circuit the multi-GB `cp -a` in main.c BEFORE the copy when the flag is set, and DROP the 'blocks-non-empty' heuristic (it would block the primary use of importing headers over an already-synced blocks table). system-cp + system-shell-quote (REVISE/ACCEPT): replace all five system("cp -a"/"rm -rf") sites with dir_copy/dir_remove_tree (skip LOCK by design, checked byte copy, fail-closed); fold — fix file path is config/src/file_ops.c, keep both intermediate mkdirs in the --importblockindex branch (dir_copy is non-recursive/leaf-only), and EXTEND to the two additional shell sites leveldb_store.c:59 and block_data.c:80 or scope them out explicitly. shared-path-nfile (ACCEPT): clamp negative file_num/data_pos/undo_pos to 0 in the header_only=false branch so one odd row cannot abort a 3M-row import; drop the prose about clearing status bits. have-data-remarked (REVISE, docs-only): document that DISK presence (hash-checked, contiguous-from-tip) owns BLOCK_HAVE_DATA and correct the misleading 'lazy P2P' comments; fix the framing that the boot scan rewrites SQLite (it mutates only the in-memory index). Depends on step 2 because these edits touch the same import functions the security work rewrites; sequence to avoid churn. Human-gated because it shares the consensus import path.

### [4] Add regression test for header-only import + idempotency + nFile boundary  — `safe-auto` (deps: 2,3)
items: no-regression-test-and-not-c-native
Verdict ACCEPT. New lib/test/src/test_snapshot_block_index_import.c (test_parallel-wired via the X-macro registry) building a real LevelDB block-index fixture and asserting: header_only clears HAVE_DATA/HAVE_UNDO + file/pos=0 while keeping nSolution/BLOCK_VALID_SCRIPTS; header_only=false with no-body rows yields file_num=0 (not -1) and does NOT abort (pins the nFile-negative fix from step 3); back-to-back imports are idempotent (DELETE FROM blocks); missing/empty index returns false without crashing. Folds: also exercise the production snapshot_import full path (shared header_only=false), assert node_db_sync_set_tip reset, and add a comment that the clamp lives on the shared cold-import path. Comes after the import code is final (steps 2-3) so the test pins the finished contract, not a moving target. Read 'N passed, M failed' from test_parallel.

### [5] Seconds-to-tip: batch the per-block CEC fsync during catch-up  — `human-gated` (deps: 4)
items: perf-cec-per-block-fsync
REVISE (design was a no-op as drafted) — high value once corrected. Root cause confirmed: update_tip is 449-665ms/block because chain_evidence_controller_promote_tip opens and COMMITs its own node.db txn (one WAL fsync per block) when no outer txn is open; CEC already auto-joins an outer txn, so zero CEC changes are needed. REQUIRED corrections before implementing: (a) WRONG WRAP LOCATION — the per-block fsyncs happen across SEPARATE activate_best_chain calls driven by the drain loop in chain_activation_controller.c:420-444 (one block per pass) and the do/while at activate_best_chain.c:478, NOT the path[] loop at 315-330; put the batch coordinator around the drain loop / do-while so ONE node.db txn spans many connect_tip calls. (b) WRONG GATE — is_initial_block_download latches FALSE at 24h-from-tip, i.e. exactly during the slow tail; replace with a lag-based predicate (network_tip - log_head over a threshold) that is true during the tail, with mandatory flush+commit at batch boundary, on shutdown (activate_best_chain.c:484 already flushes coins on shutdown — also commit the open txn), and on any early return/error. (c) ORDERING — the documented invariant is block_index(LevelDB) fsync BEFORE coins.db commit (at-tip kill-9); the draft inverted it. Explicitly bound and accept (or force ordering for) the new cec-SQLite-lags-block_index recovery shape, confirming replay reconnects from block_index. Human-gated: weakens per-block crash-recovery during catch-up; gate strictly on lag and keep per-block durability at tip. This is THE dominant tail cost (>80% of per-block time).

### [6] Seconds-to-tip: import recent tail bodies from zclassicd blk files  — `human-gated` (deps: 4)
items: perf-import-recent-bodies-from-zclassicd
REVISE — sound mechanism, one load-bearing fix. Fold the bounded tail-body import into the same single LevelDB pass the index import already runs (it already has each block's source nFile/nDataPos). For heights in [body_floor, body_ceiling], read the body from the SOURCE blocks dir via read_block_from_disk_pread and write it into the local store; connect_block fully revalidates on apply, so a torn/mis-positioned body self-heals to P2P. REQUIRED fix: write_block_to_disk does NOT allocate a slot — you MUST call find_block_pos(&local_pos, size, datadir) per block FIRST (the draft wrote every body at offset 0 of blk00000, corrupting all positions). Also: use params->pchMessageStart not the file-static ZCL_MAGIC; derive body_ceiling = source_max_height - safety_margin via a pre-scan (the pass is hash-ordered so source max is unknown mid-pass); source_blocks_dir must be the ORIGINAL snap_dir (the LOCK-copy temp excludes blk*.dat). Test must read BOTH in-window blocks back by their recorded positions to catch the overwrite class. Human-gated (shares import + consensus read path). Replaces single-peer ~14 blk/min P2P tail with a local sequential copy.

### [7] Seconds-to-tip: debounce catchup-job churn + guard boot full-map walks + delete dead ibd_throttle  — `safe-auto` (deps: 4)
items: perf-catchup-batch-churn, perf-boot-fullmap-walk-after-headeronly, perf-ibd-throttle-dead-code
perf-catchup-batch-churn (ACCEPT): debounce the catchup-job spawn in projection_backfill_service_thread — only spawn when lag>=64 or >=30s since last spawn; collapses dozens of tiny per-5-block jobs (each paying private-DB open + BEGIN/COMMIT probe + mmap) into ~2/min near tip; at tip nothing spawns. Pure scheduler subtraction, no consensus code. perf-boot-fullmap-walk (REVISE): short-circuit the O(3.1M) body-replay walks when there are no local bodies; REQUIRED fixes — bound the probe to [1,c->height] via active_chain_at (active_chain_height can exceed the array bound -> OOB read), and for backfill_shielded use a MAP-based any-body probe (reuse boot's scan_max_have_data_h) not the active-chain probe (backfill iterates the full map incl. forks); DROP the shielded_backfilled flag-ordering item (the flag is write-only/irrelevant). perf-ibd-throttle (ACCEPT, REVISE): delete the entire dead ibd_throttle module (acquire path has zero callers); REQUIRED — also remove the stale #include in config/src/boot_services.c:127 (compile error otherwise) and the docs/RUNBOOK.md:409 EV_IBD_THROTTLED row; keep disk_monitor. All independent of the cutover; safe-auto.

### [8] Seed UTXO projection from the import anchor (CUTOVER keystone gate)  — `human-gated` (deps: 1,2)
items: utxo-projection-not-seeded-from-import-anchor
BLOCKER, REVISE. The keystone utxo_commitment gate requires legacy coins.db SHA3 == utxo_projection SHA3 byte-for-byte, but the import wrote ~1.35M UTXOs straight into coins.db with NO EV_UTXO_ADD, so the projection only has tail deltas and the gate can NEVER pass. Fix: one-time idempotent anchor seed that bulk-copies coins.db `utxos` into the projection `utxo` table (byte-identical 6-column LE serialization, ORDER BY txid,vout) then advances the cursor to event-log head. CRITICAL fold (verified against the LIVE datadir): the live utxo_projection.db ALREADY has 43 tail rows and NO seeded flag — so the draft's 'non-empty -> skip + latch flag' guard would brick the gate forever. Gate SOLELY on a seeded_from_anchor flag; when unseeded, DELETE the utxo table + reset cursor to 0, copy the anchor, then re-fold tail deltas from 0. Add a regression that pre-populates the projection with tail rows + no flag and asserts count==legacy_count. Human-gated (touches authority comparison surface); depends on the import being final (step 2) and on a readable preflight (step 1).

### [9] Seed header_admit_log from the canonical chain (CUTOVER header gate)  — `human-gated` (deps: 1,2)
items: header-admit-diff-no-events-for-anchor, validate-headers-window-coverage
BLOCKER, REVISE. Corrected premise (verified): the header_ready gate binds to the header_admit STAGE progress.kv (header_admit_log + stage_cursor), NOT to EV_BLOCK_HEADER. The shadow stage would re-walk 3.12M from cursor 0 at ~250 hdr/s (~3.5h) — that is the symptom. Fix: seed header_admit_log + the header_admit stage_cursor in the same import pass from the (height,hash,parent) rows, so the diff converges trivially and the cursor == tip+1 immediately. CRITICAL fold: seed STRICTLY from the canonical/most-work chain (walk prev_hash from the selected tip), NOT raw LevelDB 'b'-iteration order — a real zclassicd index contains stale forks at shared heights; INSERT OR REPLACE by height with arbitrary hash-ordered iteration would seed a non-canonical hash and the live gate (compares vs active_chain_at) reports mismatch. Test fixture MUST include a competing fork at a shared height (a synthetic linear index would falsely pass). validate-headers-window-coverage (ACCEPT, test-only): the validate_headers gate is already correct and downstream of header_admit — add a regression that an incomplete window blocks and a full window clears; no production change. Human-gated; depends on import final + readable preflight.

### [10] Drive shadow conservation diffed counter from a background Job  — `human-gated` (deps: 1)
items: conservation-diffed-never-driven
BLOCKER, REVISE. conservation_ok requires fed==diffed, but record_diffed is bumped ONLY by an MCP handler, so under normal -shadow operation fed climbs while diffed stays 0 -> permanent blocker. Add a supervised observe-only Job (modeled on header_probe_poll, lives in app/jobs = Job shape for gate E10, registered under -shadow in boot_shadow_feeder_start, benign LOCK tolerance). CRITICAL folds (the draft re-created the bug): (a) the shadow log is SPARSE on an import-anchored datadir (only the ~760 observed tail blocks) — diffing from height 0 short-circuits at SHADOW_MISSING with checked_count=0; start the diff at the shadow log's actual FLOOR (add a min_height accessor to block_log_port/blf), not 0. (b) Reconcile counter currency: fed is a per-observation counter (reorgs double-bump via hash-dedup), checked_count is distinct-height; make record_fed a distinct-height set OR compute diffed from the same height-set fed, so fed==diffed is reachable and reorg-stable. Use the live-smoke assertion (diffed tracks fed) as the acceptance gate. Human-gated (shadow comparison surface). Independent of steps 8-9 except sharing -shadow boot wiring; depends only on a readable surface (step 1).

### [11] Verify guard condition + correct the live/chain-advance tip_recent gate  — `safe-auto` (deps: 1)
items: guard-condition-must-be-registered-and-clean, live-and-chain-advance-gates-tail-progress
guard-condition (ACCEPT, test-only): registration is already live/unconditional (condition_registry_register_all at boot_services.c:2967) with config + pristine state matching the gate; add a test pinning the controller's config_ready/state_ready predicate so a future severity/poll edit can't silently flip the live gate. live-and-chain-advance (ACCEPT): mostly operational (gates self-satisfy when sync finishes) EXCEPT one real defect — tip_recent requires tip_advance_age<=180s UNCONDITIONALLY, but at 75s Poisson spacing ~9% of blocks exceed 180s, flipping the gate RED on a healthy at-tip node on a coin flip; the runtime guard cutover_no_forward_progress already only treats stale age as a problem when BEHIND. Fix: gate tip_recent on lag (only require recent-connect when tip_lag>0; at tip_lag==0 rely on !tip_stale). Fold: describe tip_lag==0 as 'not known to be behind' (zero-init covers unavailable peer heights); add the behind-but-stale-peers test case. Safe-auto, narrow, never weakens the behind case. Independent; depends only on step 1 to verify live.

### [12] Harden the unattended soak canary (loud page + N-block/N-second soak)  — `human-gated` (deps: 8,9,10,11)
items: unattended-soak-canary-warn-not-loud
REVISE, human-gated. For an unattended soak before the flip: (a) a divergence currently auto-reverts but only WARN-logs (silent-ish, the 2026-05-25 'operator-needed routes to nobody' concern) because the engine only pages when a remedy stays UNWITNESSED and the canary's revert immediately witnesses-clear — fix by emitting EV_OPERATOR_NEEDED directly from the divergence remedy (the chain_tip_watchdog idiom that alerts.c latches to log+webhook sinks) and raise severity to CRITICAL for honest labelling. (b) Widen the 'passed' target from tip+1 to an N-block AND N-second soak so authority over 3.1M blocks is not flipped on one block. CRITICAL folds: also widen the `failed` deadline in lockstep (currently changed_at+180s) or a HEALTHY multi-minute soak reports canary_failed=true (false negative violating live-truth doctrine); and UNIFY the two independent hardcoded tip+1 targets in cutover_controller_preflight.c:193 and cutover_controller.c:146 with cutover_modes' new target, updating test_cutover_* fixtures. Comes after the gates can converge (8-11) since the soak only matters once preflight can go green. Human-gated; the flip itself stays separate.

### [13] Confirm cutoverpreflight GREEN on the clean at-tip import-anchored datadir  — `safe-auto` (deps: 5,6,7,8,9,10,11,12)
Verification milestone, no code. With the import sound (2-4), seconds-to-tip real so the tail actually finishes and tip_lag->0 (5-7), the projections seeded (8-9), conservation driven (10), guard + live gate correct (11), and the soak canary hardened (12), all 8 ANDed preflight gates (live, chain_advance, guard, header, validate, modes, conservation, utxo_commitment) can be satisfied from clean-data + at-tip WITHOUT an operator hand-driving RPCs. Read the live blockers array via the now-working MCP (step 1) and confirm ready=true with an empty blockers array. This is the precondition the flip is gated on; it is itself safe (read-only) but its inputs are everything above.

### [14] THE LIVE-CHAIN AUTHORITY FLIP (cutovermode all authoritative)  — `human-gated` (deps: 13)
ALWAYS human-gated — the single most safety-critical action. Only after preflight is GREEN (13) and an unattended soak with the hardened canary (12) has held long enough to give a confident go. Flip authority from coins.db to the event log: change the compile-time default at cutover_modes.c:16 (g_header_pipeline_modes initializer) + the :249 reset to the authoritative bitmask and retire cutover_modes_revert_all_to_shadow at :67 (NOT the stage helpers at header_admit_stage.c:185 etc — those only READ the modes; the gate0-wrong-default-location verdict confirms editing the stages would leave the pipeline in SHADOW after a supposed permanent flip). The canary auto-reverts on divergence and the no-forward-progress condition reverts on stall, so the flip is recoverable. Requires explicit operator go; the orchestrator never flips the live chain autonomously.

### [15] Post-flip soak before any deletion  — `human-gated` (deps: 14)
Human-gated observation window. After the flip, soak the authoritative event-log path on the live chain (tip advancing, projection==replay invariant holding, no divergence, no stall) long enough to trust it as the sole writer before deleting the legacy comparison apparatus. Resilience doctrine: forward-progress on the LIVE tip is the gate, not green tests. B8 deletion (step 16) MUST NOT begin until this soak passes.

### [16] B8: delete the comparison apparatus (closes much of D5/E6/E7)  — `human-gated` (deps: 15)
items: mcp-metrics-shadow-divergence-orphan, gate0-wrong-default-location, stale-boot-wiring-linerefs-step4, stale-condition-registry-linerefs, test-c-only-block-not-listed, conservation-orphan-list-incomplete-but-covered
Execute the push-button deletion plan (docs/work/b8-comparison-apparatus-deletion-plan.md) ONLY after the post-flip soak. First land the doc-correctness fixes that make it mechanically safe (all ACCEPT/verified): gate0-wrong-default-location (point Gate-0 #1 at cutover_modes.c:16,249,67 not the stage helpers — already done in step 14, reconcile the doc); mcp-metrics-shadow-divergence-orphan (add the metrics.h/.c shadow-divergence gauge deletion — a dead Prometheus surface the plan omits); stale-boot-wiring-linerefs-step4 (correct Step-4 line numbers and add the trap note that only boot_shadow_feeder_* is apparatus while boot_*_phase4_storage_shadow is KEEP); test-c-only-block-not-listed (add test.c:239-245 ZCL_TEST_ONLY handler to the Step-5 removal list, else compile break); stale-condition-registry-linerefs + conservation-orphan-list (low/cosmetic doc reconciliation). Then run Steps 1-5 (flip->confirm done, delete preflight before conservation per ordering). Deletion removes ~28 legacy E6 write surfaces and the apparatus E1/E2 entries. Human-gated because it is the irreversible removal of the safety-comparison net; verify make-lint-clean + test_parallel green after.

### [17] E1: extend size gate to config/ and baseline the boot mega-modules  — `safe-auto` (no deps)
items: e1-config-megamodules-exempt
ACCEPT, INDEPENDENT of the cutover — start immediately in parallel. The E1 gate scans `find app` only, exempting the three largest hand-written .c files (config/src/boot.c 3366, boot_services.c 3330, boot_index.c 1499). Extend to `find app config` and baseline these three at current LOC (pure ratchet, zero code change, only-shrinks). Aligns E1 with peer gates (check_one_write_path/check_no_secret_printf already scan config/). DEFER the boot-file split — the FRAMEWORK endpoint is dissolution into app/supervisors/ Supervisor shapes, and an arbitrary split touches load-bearing enum boot_stage ordering for no gain. Note the sibling check_long_functions.sh still exempts boot files (separate gap). No build/boot risk; reversible.

### [18] E6: split the one-write-path baseline (sanctioned vs legacy) so the legacy half can ratchet to empty  — `safe-auto` (no deps)
items: e6-pattern-conflates-sanctioned-writer
REVISE, INDEPENDENT — do the split NOW (the deletion of the legacy half completes after B8/step 16). Of 64 E6 entries, 38 are the SANCTIONED permanent writer (lib/validation connect/disconnect/activate + process_new_block ingress in miner/net/mining_controller + coins_view_sqlite single-writer + utxo_projection_set_author) and 26 are legacy-to-delete (coins_view_cache_flush in controllers, active_chain_set_tip in chain_state_repository/chain_tip, boot flushes). Split into two baseline files with a pin-pass that fails loud if a sanctioned line disappears (strictly stronger enforcement). REQUIRED folds: build the pin-pass `seen` set with the byte-identical sed-space-collapse normalization the runtime key uses (plant a multi-space sanctioned line in a test); update the two stale references Makefile:985 and the b8-plan:69. A pure path/pattern filter cannot do this (mis-classifies both directions). Without the split E6 can never reach empty by waiting on B8.

### [19] Reconcile REFACTOR_STATUS baseline numbers + add a machine-checked count assertion; graduate E7 to HARD; dispose E2 survivors  — `safe-auto` (deps: 16,17,18)
items: refactor-status-stale-baseline-numbers, d5-misclassifies-oversized-files, e6-e7-graduate-to-hard, e2-survivors-need-disposition
Doc/honest-promise hardening, after B8 (16) and the E1/E6 baseline changes (17-18) settle the live numbers. refactor-status-stale (REVISE): correct prose counts to live (E1=4->7 after step 17, E2=9, E6=64, E7=0) using `grep -vc '^#\|^$'`; extend check_doc_accuracy.sh (E11) with a <!--BASELINE-COUNTS--> block asserting expected==actual per file (fold: add `|| true` to the grep -vc on the 0-count entry under set -e; also fix the stale line-138 LOC). d5-misclassifies (REVISE): correct the false 'B8 deletes all 4 oversized files' claim — legacy_import.c, sync_controller_catchup.c, sync_controller_import.c are KEEP independent splits (schedule D5a-c along existing internal-header seams); only legacy_mirror_sync_service shrinks via C2; fold — DROP the phantom block_source_policy_runtime.c (it is 460 LOC, not 1355, not baselined) and fix the count to 4. e6-e7-graduate (REVISE): graduate E7 to HARD now (baseline already 0; delete the baseline-read path, keep the //ram-state-ok escape hatch); fold — update DEFENSIVE_CODING.md:617,623 (RATCHET->HARD, remove deleted-file reference) and strengthen the test to assert the baseline-read code is gone; defer E6-legacy HARD flip to post-soak. e2-survivors (ACCEPT): label the 9 E2 survivors by intent — the 4 block_source_policy_* files are decision-predicates/void (use the //one-result-type-ok override, NOT a wrong zcl_result conversion), cutover_modes retires with B8, the rest are B-entangled; fold — reconcile the doc's stale '6' (real is 9) to the post-change 5. All safe-auto doc/gate work.

## Human gates

- Step 2 — Trust-model / commitment-ceremony decision: choosing whether to (a) refuse/quarantine an above-checkpoint imported UTXO set until a fresh SHA3 commitment is minted at/near the import height and made FATAL, or (b) accept the operator's own zclassicd as a documented trust boundary with only PoW spot-check + header binding. This is a consensus/money trust-boundary call for a sovereign node and the connect_tip whole-table-SHA3 broadening must NOT be shipped. Requires explicit operator go.
- Steps 2,3,6 — Any change to the consensus import path (import_block_index_thread, utxo_recovery, tail-body import, find_block_pos slot allocation) that affects how the live tip is established; verify against the running chain, never auto-apply, never stop zclassicd.
- Step 5 — Batching durable tip writes during catch-up weakens per-block crash-recovery ordering (block_index-before-coins kill-9 invariant); gate strictly on lag, flush+commit on boundary/shutdown/error, keep per-block durability at tip.
- Steps 8,9,10 — Seeding the event-log projections (UTXO anchor, header_admit_log) and adding a background diff Job touch the authority-comparison surface that the flip depends on; verify the keystone gate goes green from clean data without operator-driven RPCs.
- Step 12 — Tightening canary severity and soak target changes auto-revert behavior; must not introduce flip<->revert flapping and must keep canary_failed honest during a healthy multi-minute soak.
- Step 14 — THE LIVE-CHAIN AUTHORITY FLIP (cutovermode all authoritative). ALWAYS human-gated. Only after preflight GREEN (13) and a passing soak. Never performed autonomously by the orchestrator.
- Step 15 — Post-flip soak sign-off: forward-progress on the LIVE tip (not green tests) is the gate before any legacy deletion.
- Step 16 — B8 deletion of the comparison apparatus is irreversible removal of the safety-comparison net; only after the post-flip soak passes, with make-lint-clean + test_parallel green verified after.

## Open questions / blockers

- BLOCKER (step 2): There is NO enforced SHA3 UTXO commitment at or above the import height (~3128361); the only commitment is at 3056758, below it. Until either a fresh commitment is minted at/near the import height (a human-run release ceremony like the 2026-03-26 checkpoint) and made FATAL, OR the import refuses/quarantines an unverifiable above-checkpoint set, a tampered ~/.zclassic chainstate installs spendable forged money. Coin authority flows through the live `utxos` table, NOT the tip cursor — withholding commit_tip does NOT prevent spending. This must be resolved before the import can be trusted as the foundation for the flip.
- BLOCKER (step 5): The seconds-to-tip catch-up batch fix's correct wrap point (drain loop in chain_activation_controller.c vs do/while in activate_best_chain.c) and lag-based gate must be implemented and measured to actually reduce the observed 449-665ms update_tip during the tail; the original design wrapped the wrong loop and used a gate (is_initial_block_download) that latches false exactly during the slow tail, making it a no-op. Confirm the tail is still pre-tip (lag>0) when the 470ms cost is observed.
- BLOCKER (steps 8,9): The header_admit and UTXO-projection seeds must seed from the CANONICAL/most-work chain, not raw LevelDB iteration order — a real zclassicd index contains stale forks at shared heights that would seed non-canonical hashes and make the live diff report mismatch. The UTXO-projection seed must gate solely on a seeded_from_anchor flag (NOT row-count emptiness) or it permanently bricks the keystone gate on the live datadir (which already has 43 tail rows + no flag).
- BLOCKER (step 10): The conservation Job must diff from the SPARSE shadow log's actual floor (needs a new min_height accessor) and reconcile fed (per-observation, reorg-double-counted) vs checked_count (distinct-height) currencies, or fed==diffed is structurally unreachable and the gate stays red forever.
- DEPENDENCY: The reproducible-signed-build posture (security gap, severity medium, NOT in the ordered steps because it is orthogonal to reaching FINISHED tip/flip) is BLOCKED on a false premise — the vendored static archives (libcrypto/libssl/libleveldb/libevent, ~13MB) are .gitignore'd, not committed/hash-pinned. A reproducible-build claim is hollow until those are committed or recorded by SHA3 in BUILDINFO with a verify step. Decide whether to schedule this as a separate workstream; it does not gate the node reaching the live tip but is required for the CLAUDE.md 'reproducible signed releases' pillar to be honest.
- VERIFICATION: All gate-convergence (step 13) and live-tail-completion claims rest on the MCP 401 being resolved (step 1). Until the live blockers array is readable, gate status is source-inferred only. Confirm each of the 8 preflight gates live before declaring GREEN.