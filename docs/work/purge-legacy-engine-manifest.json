{
  "summary": "Inventory the legacy coins.db engine + comparison apparatus to rip out, and define the single log/projection/Job engine that stays",
  "agentCount": 6,
  "logs": [],
  "result": {
    "inv": [
      {
        "area": "SWEEP 1 — comparison apparatus (shadow feeder/mutator + diff use cases + cutover machinery + their MCP/RPC handlers, -shadow flag, and tests)",
        "delete_files": [
          {
            "file": "adapters/inbound/src/shadow_feeder.c",
            "reason": "Inbound adapter that exists ONLY to mirror every ingested block into a parallel blocks.shadow log + fire VALIDATE_BLOCK onto the mutator so the diff path can compare. Pure apparatus.",
            "referenced_by": [
              "adapters/inbound/src/shadow_feeder_global.c (calls shadow_feeder_observe_block)",
              "config/src/boot_services.c (boot_shadow_feeder_start/dispose, lines ~165-235, 200,230)",
              "lib/net/src/msg_blocks.c (forward extern + call at lines 269,411 — REWIRE: delete the observe call)",
              "lib/test/src/test_shadow_feeder.c",
              "lib/test/src/test_shadow_conservation.c"
            ]
          },
          {
            "file": "adapters/inbound/include/adapters/inbound/shadow_feeder.h",
            "reason": "Header for the deleted shadow_feeder.c.",
            "referenced_by": [
              "adapters/inbound/src/shadow_feeder.c",
              "adapters/inbound/src/shadow_feeder_global.c",
              "config/src/boot_services.c (#include line ~159)"
            ]
          },
          {
            "file": "adapters/inbound/src/shadow_feeder_global.c",
            "reason": "Process-global singleton wrapper around shadow_feeder so the hot net ingest path (msg_blocks.c) can fire-and-forget observe without threading state. Apparatus.",
            "referenced_by": [
              "config/src/boot_services.c (shadow_feeder_global_set at 210,221)",
              "lib/net/src/msg_blocks.c (shadow_feeder_global_observe at 411)",
              "tools/mcp/controllers/chain_controller.c (shadow_feeder_global_is_active at 126 — handler is also deleted)",
              "lib/test/src/test_shadow_feeder_global.c"
            ]
          },
          {
            "file": "adapters/inbound/include/adapters/inbound/shadow_feeder_global.h",
            "reason": "Header for deleted shadow_feeder_global.c.",
            "referenced_by": [
              "adapters/inbound/src/shadow_feeder_global.c",
              "config/src/boot_services.c (#include line ~160)"
            ]
          },
          {
            "file": "adapters/inbound/src/shadow_conservation.c",
            "reason": "Process-global fed/diffed/skipped counters whose SOLE purpose is the 'fed==diffed' conservation law proving every block fed to the shadow pipeline was diffed against legacy. No counter gates anything once the duality is gone.",
            "referenced_by": [
              "adapters/inbound/src/shadow_feeder.c (record_fed/record_skipped)",
              "app/jobs/src/conservation_diff_job.c (record_diffed at 275)",
              "app/services/src/cutover_modes.c (shadow_conservation_ok at 228 — file also deleted)",
              "app/controllers/src/cutover_controller_preflight.c (shadow_conservation_ok at 379 — file also deleted)",
              "tools/mcp/controllers/chain_controller.c (record_diffed/ok at 189,227 — handler deleted)",
              "lib/test/src/test_shadow_conservation.c",
              "lib/test/src/test_rpc.c (333,477-479,524)"
            ]
          },
          {
            "file": "adapters/inbound/include/adapters/inbound/shadow_conservation.h",
            "reason": "Header for deleted shadow_conservation.c.",
            "referenced_by": [
              "adapters/inbound/src/shadow_feeder.c",
              "app/jobs/src/conservation_diff_job.c",
              "app/services/src/cutover_modes.c",
              "app/controllers/src/cutover_controller_preflight.c"
            ]
          },
          {
            "file": "application/operations/src/diff_with_legacy_shadow.c",
            "reason": "Use case that walks two block_log_ports (legacy 'primary' vs 'shadow') byte-for-byte to find the first divergent height. Exists only to compare the two engines.",
            "referenced_by": [
              "application/operations/src/shadow_replay_proof.c (calls it)",
              "tools/shadow_replay_proof.c",
              "tools/mcp/controllers/chain_controller.c (h_zcl_diff_with_legacy_shadow at 181)",
              "lib/test/src/test_diff_with_legacy_shadow.c",
              "lib/test/src/test_shadow_replay_full_driver.c",
              "lib/test/src/test.c + lib/test/src/test_parallel.c (test registration)"
            ]
          },
          {
            "file": "application/operations/include/application/operations/diff_with_legacy_shadow.h",
            "reason": "Header for the deleted diff use case (struct diff_with_legacy_shadow_inputs/report, DIFF_STATUS_* enum).",
            "referenced_by": [
              "application/operations/src/diff_with_legacy_shadow.c",
              "application/operations/src/shadow_replay_proof.c",
              "tools/mcp/controllers/chain_controller.c",
              "tools/shadow_replay_proof.c"
            ]
          },
          {
            "file": "application/operations/src/shadow_replay_proof.c",
            "reason": "Use case that re-feeds the legacy primary log into an empty shadow log then runs diff_with_legacy_shadow to PROVE convergence. Pure cutover-proof apparatus.",
            "referenced_by": [
              "tools/shadow_replay_proof.c",
              "lib/test/src/test_shadow_replay_proof.c",
              "lib/test/src/test_reorg_parity.c",
              "lib/test/src/test_shadow_replay_full_driver.c",
              "lib/test/src/test.c + lib/test/src/test_parallel.c"
            ]
          },
          {
            "file": "application/operations/include/application/operations/shadow_replay_proof.h",
            "reason": "Header for deleted shadow_replay_proof.c.",
            "referenced_by": [
              "application/operations/src/shadow_replay_proof.c",
              "tools/shadow_replay_proof.c",
              "lib/test/src/test_shadow_replay_proof.c"
            ]
          },
          {
            "file": "tools/shadow_replay_proof.c",
            "reason": "Standalone CLI tool wrapping shadow_replay_proof_run + diff_with_legacy_shadow + block_log_legacy — an offline two-engine convergence prover. Apparatus.",
            "referenced_by": [
              "Makefile (build target for the tool binary — SHRINK build rules)"
            ]
          },
          {
            "file": "app/jobs/src/conservation_diff_job.c",
            "reason": "Job whose every step is one shadow-log read + one zclassicd getblock RPC + a byte compare + cursor bump — its only output is the 'diffed' conservation counter. Exists solely to compare shadow vs legacy in the background.",
            "referenced_by": [
              "config/src/boot_services.c (conservation_diff_job_shutdown at 3167, #include at 25)",
              "app/supervisors/src/staged_sync_supervisor.c (staged_conservation_diff_register at 439-462, drain/cursor at 423-436, called at 482)",
              "app/controllers/src/diagnostics_registry.c (dump-state table entry at 447, #include at 51)",
              "lib/test/src/test_conservation_diff_job.c",
              "lib/test/src/test.c + lib/test/src/test_parallel.c"
            ]
          },
          {
            "file": "app/jobs/include/jobs/conservation_diff_job.h",
            "reason": "Header for deleted conservation_diff_job.c.",
            "referenced_by": [
              "config/src/boot_services.c",
              "app/supervisors/src/staged_sync_supervisor.c",
              "app/controllers/src/diagnostics_registry.c"
            ]
          },
          {
            "file": "app/services/src/cutover_modes.c",
            "reason": "Holds the per-stage SHADOW/AUTHORITATIVE bitmask + canary anchor + cutover_dump_state_json (zcl_state subsystem=cutover). The entire shadow/authoritative duality lives here; once the staged pipeline runs unconditionally there is no mode to flip.",
            "referenced_by": [
              "app/jobs/src/header_admit_stage.c (set/get at 298-309)",
              "app/jobs/src/validate_headers_stage.c (set/get at 487-496)",
              "app/jobs/src/tip_finalize_stage.c (set/get at 592-604)",
              "app/conditions/src/cutover_no_forward_progress.c (set_header_pipeline)",
              "app/conditions/src/cutover_canary_complete.c (canary_target_reached/snapshot/revert)",
              "app/controllers/src/cutover_controller.c",
              "app/controllers/src/cutover_controller_preflight.c",
              "app/controllers/src/diagnostics_registry.c (zcl_state cutover dumper)",
              "tools/scripts/one_result_type_baseline.txt line 23"
            ]
          },
          {
            "file": "app/services/include/services/cutover_modes.h",
            "reason": "Header for deleted cutover_modes.c (cutover_stage_mode_t, canary snapshot struct, CUTOVER_CANARY_WATCH_SECS).",
            "referenced_by": [
              "all cutover_modes.c callers above"
            ]
          },
          {
            "file": "app/controllers/src/cutover_controller.c",
            "reason": "The cutovermode RPC: flips stage modes shadow<->authoritative and (critically) drives utxo_projection_set_author() at lines 278-280. After the purge the staged pipeline is unconditionally authoritative, so this control RPC has no reason to exist.",
            "referenced_by": [
              "app/controllers/src/diagnostics_controller.c (RPC table 'cutovermode' at line 32)",
              "app/controllers/src/cutover_controller_preflight.c (cutover_preflight_ready_now reads it; shared decls in diagnostics_internal.h)",
              "app/controllers/include/controllers/diagnostics_internal.h (diag_rpc_cutovermode, cutover_push_canary_state decls at 42-54)",
              "tools/mcp/controllers/ops_controller.c (h_zcl_cutovermode at 295)"
            ]
          },
          {
            "file": "app/controllers/src/cutover_controller_preflight.c",
            "reason": "The cutoverpreflight RPC: a read-only 'is it safe to flip authority?' gate ANDing the conservation law, header-admit diff, utxo_commitment parity, etc. Meaningless once there is one engine.",
            "referenced_by": [
              "app/controllers/src/diagnostics_controller.c (RPC table 'cutoverpreflight' at line 33)",
              "app/controllers/src/cutover_controller.c (cutover_preflight_ready_now -> diag_rpc_cutoverpreflight)",
              "app/controllers/include/controllers/diagnostics_internal.h (diag_rpc_cutoverpreflight at 47)",
              "app/controllers/src/diagnostics_registry.c (cutoverpreflight help text refs at 463,485)",
              "tools/mcp/controllers/ops_controller.c (h_zcl_cutoverpreflight at 324)",
              "lib/test/src/test_cutover_preflight.c"
            ]
          },
          {
            "file": "app/controllers/src/cutover_controller_preflight.h_NONE",
            "reason": "(no separate header — shared decls live in diagnostics_internal.h; listed only for completeness, nothing to delete here)",
            "referenced_by": [
              "app/controllers/include/controllers/diagnostics_internal.h"
            ]
          },
          {
            "file": "app/conditions/src/cutover_no_forward_progress.c",
            "reason": "Condition that auto-reverts the header pipeline shadow when an authoritative cutover stops making forward progress. Has no meaning when there is no shadow mode to revert to.",
            "referenced_by": [
              "app/conditions/src/condition_registry.c (decl at 23, register call at 49)",
              "lib/test/src/test_watchdog_conditions_pr3.c",
              "lib/test/src/test_cutover_autorevert.c"
            ]
          },
          {
            "file": "app/conditions/src/cutover_canary_complete.c",
            "reason": "Condition that watches the post-flip canary block and either locks in authoritative or reverts to shadow on divergence. Cutover-only.",
            "referenced_by": [
              "app/conditions/src/condition_registry.c (decl at 24, register call at 50)",
              "lib/test/src/test_watchdog_conditions_pr3.c",
              "lib/test/src/test_cutover_autorevert.c"
            ]
          },
          {
            "file": "app/controllers/src/projection_diff_controller.c",
            "reason": "Implements getmirrorstatus + the seven *projectiondiff RPCs, each of which compares a pure projection against the legacy node.db table. The projections STAY; their diff-vs-legacy comparison is apparatus. (Confirm getmirrorstatus disposition with SWEEP-2 legacy_mirror owner; the projectiondiff handlers here are unambiguously apparatus.)",
            "referenced_by": [
              "app/controllers/src/diagnostics_controller.c (RPC table entries 37-47)",
              "app/controllers/include/controllers/diagnostics_internal.h (diag_rpc_*projectiondiff + getmirrorstatus decls at 70-86)",
              "tools/mcp/controllers/diagnostics_controller.c (h_zcl_*_projection_diff handlers + tool table 629-658)",
              "lib/storage/src/contacts_projection.c / onion_announcements_projection.c / hodl_history_projection.c (the *_projection_diff_legacy callees — STRIP those legacy-diff fns)"
            ]
          },
          {
            "file": "mutator/src/mutator.c",
            "reason": "Worker-thread command queue whose ONLY command kind (MUTATOR_CMD_VALIDATE_BLOCK) is pushed exclusively by shadow_feeder.c. Once the feeder is gone the mutator has zero producers.",
            "referenced_by": [
              "config/src/boot_services.c (mutator_start/stop at 174,189-205,233 inside boot_shadow_feeder_start/dispose)",
              "adapters/inbound/src/shadow_feeder.c (mutator_input_queue_push)",
              "lib/test/src/test_mutator.c",
              "lib/test/src/test_shadow_feeder.c / test_shadow_feeder_global.c / test_shadow_conservation.c"
            ]
          },
          {
            "file": "mutator/src/cmd.c",
            "reason": "Completion primitive for mutator commands. Dead with the mutator.",
            "referenced_by": [
              "mutator/src/mutator.c",
              "mutator/include/mutator/cmd.h consumers"
            ]
          },
          {
            "file": "mutator/src/input_queue.c",
            "reason": "Bounded MPSC queue for the mutator. Dead with the mutator.",
            "referenced_by": [
              "mutator/src/mutator.c",
              "adapters/inbound/src/shadow_feeder.c (mutator_input_queue_of/push)"
            ]
          },
          {
            "file": "mutator/include/mutator/mutator.h",
            "reason": "Header for deleted mutator.c.",
            "referenced_by": [
              "mutator/src/mutator.c",
              "config/src/boot_services.c (#include at 161)",
              "adapters/inbound/src/shadow_feeder.c"
            ]
          },
          {
            "file": "mutator/include/mutator/cmd.h",
            "reason": "Header for deleted mutator command/completion types (struct mutator_cmd, MUTATOR_CMD_VALIDATE_BLOCK).",
            "referenced_by": [
              "mutator/src/cmd.c",
              "mutator/src/mutator.c",
              "adapters/inbound/src/shadow_feeder.c"
            ]
          },
          {
            "file": "mutator/include/mutator/input_queue.h",
            "reason": "Header for deleted input_queue.c.",
            "referenced_by": [
              "mutator/src/input_queue.c",
              "mutator/src/mutator.c",
              "adapters/inbound/src/shadow_feeder.c"
            ]
          },
          {
            "file": "lib/test/src/test_shadow_feeder.c",
            "reason": "Tests the deleted shadow_feeder.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c registration"
            ]
          },
          {
            "file": "lib/test/src/test_shadow_feeder_global.c",
            "reason": "Tests the deleted shadow_feeder_global singleton.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_shadow_conservation.c",
            "reason": "Tests the deleted conservation counters + feeder fed/skipped path.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_diff_with_legacy_shadow.c",
            "reason": "Tests the deleted diff_with_legacy_shadow use case.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_shadow_replay_proof.c",
            "reason": "Tests the deleted shadow_replay_proof use case.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_shadow_replay_full_driver.c",
            "reason": "End-to-end driver exercising shadow_replay_proof + diff_with_legacy_shadow. Apparatus test.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_reorg_parity.c",
            "reason": "Reorg-parity test built on shadow_replay_proof — proves the two engines agree across reorgs. Apparatus.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_conservation_diff_job.c",
            "reason": "Tests the deleted conservation_diff_job.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_mutator.c",
            "reason": "Tests the deleted mutator.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_preflight.c",
            "reason": "Tests cutoverpreflight RPC. Apparatus.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_flip_dryrun.c",
            "reason": "Tests the cutover flip dry-run path (cutover_modes + conservation + header_admit_stage_diff). Apparatus.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_autorevert.c",
            "reason": "Tests cutover_no_forward_progress + cutover_canary_complete auto-revert conditions. Apparatus.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_postflip_reorg.c",
            "reason": "Tests post-flip reorg behavior of the cutover machinery + conservation. Apparatus.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_tip_parity.c",
            "reason": "Tests tip parity between the two engines at cutover. Apparatus.",
            "referenced_by": [
              "lib/test/src/test.c / test_parallel.c"
            ]
          }
        ],
        "strip_symbols": [
          {
            "file": "main.c",
            "symbol": "-shadow CLI flag parse (line 1659: ctx.shadow_feeder_enabled = true)",
            "what": "Delete the `else if (strcmp(argv[i],\"-shadow\")==0) ctx.shadow_feeder_enabled = true;` branch; remove the shadow_feeder_enabled field from struct app_context. The -shadow flag is the sole enabler of the deleted shadow feeder/mutator pipeline."
          },
          {
            "file": "config/src/boot_services.c",
            "symbol": "boot_shadow_feeder_start / boot_shadow_feeder_disarm / boot_shadow_feeder_dispose (lines ~157-235) + g_shadow_feeder/g_shadow_mutator statics + includes (159-162)",
            "what": "Delete all three boot_shadow_feeder_* functions, the two file-static handles, and the shadow_feeder/shadow_feeder_global/mutator #includes. Remove their call sites: boot_shadow_feeder_start at ~3016, boot_shadow_feeder_disarm at ~3241, boot_shadow_feeder_dispose at ~3254."
          },
          {
            "file": "config/src/boot_services.c",
            "symbol": "conservation_diff_job include (25) + conservation_diff_job_shutdown() call (~3167)",
            "what": "Delete the #include and the shutdown call; the Job is gone. (Init/drain live in staged_sync_supervisor — see below.)"
          },
          {
            "file": "lib/net/src/msg_blocks.c",
            "symbol": "shadow_feeder_global_observe forward extern (269) + call (411)",
            "what": "REWIRE/strip: delete the `extern void shadow_feeder_global_observe(...)` decl and the call at ~411. The hot ingest path must no longer mirror blocks into the shadow log; net ingest feeds only the single engine."
          },
          {
            "file": "app/jobs/src/header_admit_stage.c",
            "symbol": "header_admit mode branch: authoritative_admit() call gate (279-283), header_admit_set_mode/get_mode (296-310), g_authoritative_hook + header_admit_stage_set_authoritative_hook (55-56,119-123,313-318,394-395), header_admit_stage_diff (539+), STAGE STAYS",
            "what": "Strip the cutover duality: the stage runs the authoritative admit UNCONDITIONALLY (inline authoritative_admit's body into step_admit; drop the `if (header_admit_get_mode()==AUTHORITATIVE)` guard at 279). Remove set/get_mode (they only proxied cutover_modes), the test-only authoritative_hook, and header_admit_stage_diff (the log-vs-active_chain comparison used only by cutover preflight + zcl_diff_staged_header_admit). Keep step_admit/log_insert/init/step_once/shutdown."
          },
          {
            "file": "app/jobs/src/validate_headers_stage.c",
            "symbol": "validate_headers mode branch + validate_headers_set_mode/get_mode (487-496), authoritative-mark path (~417), init log of mode (481)",
            "what": "Strip duality: always perform the authoritative header mark; drop the shadow/authoritative gate and the set/get_mode proxies into cutover_modes. Keep the stage body, init, step_once, shutdown."
          },
          {
            "file": "app/jobs/src/tip_finalize_stage.c",
            "symbol": "is_authoritative() (485-489), tip_finalize_set_mode/get_mode (590-604), active_chain_authority.is_authoritative wiring (525), init 'shadow mode' log (550)",
            "what": "Strip duality: tip_finalize is the SOLE writer unconditionally — replace is_authoritative() with a constant true (or drop the field from active_chain_authority and treat as always-authoritative). Remove set/get_mode proxies. Keep step_finalize, set_tip_cb, init, step_once, shutdown, set_authoritative_tip (rename if desired)."
          },
          {
            "file": "app/jobs/include/jobs/header_admit_stage.h",
            "symbol": "header_admit_mode_t enum (66-68), header_admit_set_mode/get_mode (72-73), header_admit_stage_set_authoritative_hook (112), header_admit_stage_diff decl (185) + header_admit_diff_* report/sample/enum types",
            "what": "Delete the mode enum + set/get_mode + authoritative_hook decls + the diff report/sample structs/enum + header_admit_stage_diff decl. Keep init/step_once/drain/shutdown decls."
          },
          {
            "file": "app/jobs/include/jobs/validate_headers_stage.h",
            "symbol": "validate_headers_mode_t enum (99-101), set/get_mode (103-104)",
            "what": "Delete the mode enum and set/get_mode decls. Keep the stage lifecycle decls."
          },
          {
            "file": "app/jobs/include/jobs/tip_finalize_stage.h",
            "symbol": "tip_finalize_mode_t enum (27-29), set/get_mode (34-35)",
            "what": "Delete the mode enum and set/get_mode decls. Keep init/step_once/shutdown/last_height/set_authoritative_tip/divergence-counter decls."
          },
          {
            "file": "app/supervisors/src/staged_sync_supervisor.c",
            "symbol": "conservation_diff Job child: g_cd_contract/g_cd_id (411-412), cd_tick/cd_stall (414-437), staged_conservation_diff_register (439-462), include (25), call at 482",
            "what": "REWIRE: delete the entire conservation_diff supervisor child block and its registration call (line 482) and the #include (25). The other eight staged children stay; only the diff Job child is apparatus."
          },
          {
            "file": "app/controllers/src/diagnostics_controller.c",
            "symbol": "RPC dispatch table entries: cutovermode (32), cutoverpreflight (33), getmirrorstatus (37), *projectiondiff (38-47)",
            "what": "Delete the cutovermode + cutoverpreflight + the seven *projectiondiff table rows (and update the file header comment block at 9-17). getmirrorstatus disposition is SWEEP-2 (legacy_mirror); the projectiondiff and cutover rows are unambiguous apparatus."
          },
          {
            "file": "app/controllers/include/controllers/diagnostics_internal.h",
            "symbol": "diag_rpc_cutovermode (43), diag_rpc_cutoverpreflight (47), cutover_push_canary_state (54), diag_rpc_getmirrorstatus (70), diag_rpc_*projectiondiff decls (72-85)",
            "what": "Delete the cutover + projectiondiff function decls (and the cutover-canary shared-state comment block 50-52). These are decls for symbols whose .c files are deleted."
          },
          {
            "file": "app/controllers/src/diagnostics_registry.c",
            "symbol": "conservation_diff dump-state table entry (447) + include (51); cutoverpreflight help-text refs (463,485)",
            "what": "Delete the conservation_diff zcl_state dumper row + its #include. Strip/rewrite the two help strings that point operators at cutoverpreflight / zcl_utxo_projection_diff (those tools are gone). Also remove the `cutover` zcl_state subsystem dumper registration (cutover_dump_state_json) since cutover_modes.c is deleted."
          },
          {
            "file": "app/conditions/src/condition_registry.c",
            "symbol": "register_cutover_no_forward_progress decl (23) + register_cutover_canary_complete decl (24) + register calls (49-50)",
            "what": "REWIRE: delete the two forward decls and the two register_*() calls; both condition .c files are deleted."
          },
          {
            "file": "tools/mcp/controllers/chain_controller.c",
            "symbol": "h_zcl_diff_with_legacy_shadow (95-266), h_zcl_diff_staged_header_admit (296-~360), and their tool-table rows (1027-1039); shadow_conservation + block_log_legacy/diff includes",
            "what": "Delete both handlers, their tool registrations (zcl_diff_with_legacy_shadow, zcl_diff_staged_header_admit), and the now-orphaned includes (shadow_feeder_global.h, shadow_conservation.h, diff_with_legacy_shadow.h, block_log_legacy.h). NOTE: h_zcl_utxo_projection_diff (720-) and its zcl_utxo_projection_diff tool (1052) are also two-engine comparisons — flag for the same purge; they compare utxo_projection vs legacy utxocommitment."
          },
          {
            "file": "tools/mcp/controllers/ops_controller.c",
            "symbol": "h_zcl_cutovermode (295-322), h_zcl_cutoverpreflight (324-~352), tool-table rows zcl_cutovermode (946-953) and zcl_cutoverpreflight (955-960), p_cutovermode param array, header-comment ref (11)",
            "what": "Delete both handlers, their tool-table entries, the p_cutovermode params descriptor, and update the file-header tool list."
          },
          {
            "file": "tools/mcp/controllers/diagnostics_controller.c",
            "symbol": "DEFINE_PT *_projection_diff handlers (128-134), h_zcl_diff_with_legacy (200-) + its body, tool-table rows for the seven *_projection_diff (629-658) and zcl_diff_with_legacy (667-673), header-comment block (9-17)",
            "what": "Delete the seven projection-diff passthrough handlers + zcl_diff_with_legacy handler, their tool-table rows, and the doc comment. All are projection-vs-legacy comparisons."
          },
          {
            "file": "tools/mcp/metrics.c",
            "symbol": "g_shadow_divergence_count static (109), mcp_metrics_set_shadow_divergence (299-303), the zcl_shadow_divergence_count Prometheus emit block (809-815)",
            "what": "Delete the shadow-divergence gauge, its setter, and the Prometheus text block — the only writer (chain_controller h_zcl_diff_with_legacy_shadow) is being deleted."
          },
          {
            "file": "tools/mcp/metrics.h",
            "symbol": "mcp_metrics_set_shadow_divergence decl (231) + comment (220)",
            "what": "Delete the decl and its doc comment."
          },
          {
            "file": "lib/storage/src/contacts_projection.c",
            "symbol": "contacts_projection_diff_legacy (325-) + count_legacy_contacts helper",
            "what": "STRIP the legacy-diff function (and its private count_legacy_contacts helper): the projection STAYS as the single engine, but its compare-against-legacy-node.db function is apparatus (only caller is projection_diff_controller.c, being deleted)."
          },
          {
            "file": "lib/storage/src/onion_announcements_projection.c",
            "symbol": "onion_ann_projection_diff_legacy (281-)",
            "what": "STRIP the legacy-diff function; projection stays. Only caller is the deleted projection_diff_controller.c."
          },
          {
            "file": "lib/storage/src/hodl_history_projection.c",
            "symbol": "hodl_history_projection_diff_legacy (273-)",
            "what": "STRIP the legacy-diff function; projection stays. Only caller is the deleted projection_diff_controller.c."
          },
          {
            "file": "app/controllers/src/snapshot_controller_import.c / include/controllers/snapshot_controller.h",
            "symbol": "(already-modified working-tree files per git status — verify no shadow/cutover decl leaks)",
            "what": "Spot-check only: confirm no dangling references to deleted cutover/shadow symbols; no apparatus symbols were found in these during the sweep."
          }
        ],
        "rewire": [
          {
            "file": "lib/net/src/msg_blocks.c",
            "from": "shadow_feeder_global_observe((uint32_t)new_tip->nHeight, &blk, ...) at line ~411 (mirrors every accepted block into the shadow log)",
            "to": "Remove the call entirely. Net ingest already feeds the single engine via the normal accept path; there is no second engine to mirror into."
          },
          {
            "file": "app/supervisors/src/staged_sync_supervisor.c",
            "from": "staged_conservation_diff_register(datadir) at line 482 (registers the diff Job as a supervisor child)",
            "to": "Remove the call (and the whole cd_* block). The supervisor keeps the eight real staged children (header_admit..tip_finalize); the diff Job is not part of the single engine."
          },
          {
            "file": "config/src/boot_services.c",
            "from": "boot_shadow_feeder_start/disarm/dispose (lines 3016/3241/3254) + conservation_diff_job_shutdown (3167)",
            "to": "Remove all four calls. Boot no longer arms a shadow feeder/mutator or a conservation diff Job; it boots only the single-engine staged pipeline + projections."
          },
          {
            "file": "app/jobs/src/header_admit_stage.c",
            "from": "step_admit's `if (header_admit_get_mode()==HEADER_ADMIT_MODE_AUTHORITATIVE) authoritative_admit(...)` (279-283)",
            "to": "Inline authoritative_admit() unconditionally into step_admit — the stage always admits authoritatively (single engine), no mode check."
          },
          {
            "file": "app/jobs/src/tip_finalize_stage.c",
            "from": "active_chain_authority.is_authoritative = is_authoritative (525) returning tip_finalize_get_mode()==AUTHORITATIVE",
            "to": "Always-authoritative: set is_authoritative to a function returning true (or drop the predicate from struct active_chain_authority and have active_chain treat tip_finalize as the unconditional tip authority)."
          },
          {
            "file": "app/controllers/src/cutover_controller.c (being deleted) -> caller utxo_projection_set_author",
            "from": "cutovermode 'all' branch calling utxo_projection_set_author(UTXO_AUTHOR_STAGE/LEGACY) at 278-280 (the runtime author flip)",
            "to": "This flip becomes a fixed boot-time decision: utxo_apply/tip_finalize is the permanent UTXO_AUTHOR_STAGE author. SWEEP that establishes the boot-time set_author belongs to the utxo_author_t purge (parent task body); here, note the RPC-driven flip is removed with the controller."
          }
        ],
        "notes": "SWEEP 1 anchored in files actually read. Engine-that-stays untouched: event_log, the 8 *_projection.c, the staged Job reducer (header_admit..tip_finalize), progress.kv cursor.\n\nConfirmed-apparatus C/H (canonical tree, worktrees under ./.claude excluded): shadow_feeder{,_global,_conservation}, diff_with_legacy_shadow, shadow_replay_proof (app + tools/), conservation_diff_job, cutover_modes, cutover_controller, cutover_controller_preflight, cutover_no_forward_progress, cutover_canary_complete, projection_diff_controller, mutator/ (3 src + 3 hdr), plus 14 test files (test_shadow_*, test_diff_with_legacy_shadow, test_reorg_parity, test_conservation_diff_job, test_mutator, test_cutover_*).\n\nMutator is a clean cascade delete: its ONLY command (MUTATOR_CMD_VALIDATE_BLOCK) is pushed solely by shadow_feeder.c; remove the feeder and the mutator has zero producers (verified mutator.c dispatch + grep of mutator_input_queue_push callers).\n\nThree stage files (header_admit/validate_headers/tip_finalize) and their three headers STAY but lose their entire shadow/authoritative duality (mode enums, set/get_mode proxies into cutover_modes, the authoritative-gate `if`, the test-only authoritative_hook, and header_admit_stage_diff). They become unconditionally authoritative — the single engine.\n\nThree projection files (contacts/onion/hodl) STAY but lose their *_projection_diff_legacy functions (apparatus; only caller is the deleted projection_diff_controller.c).\n\nCross-sweep flags (NOT deleted in SWEEP 1, hand to the right sweep):\n- getmirrorstatus RPC + diag_rpc_getmirrorstatus: tied to legacy_mirror -> SWEEP 2.\n- block_log_legacy.c / block_log_legacy_rpc.c adapters: consumed by deleted apparatus (conservation_diff_job, diff handlers, tools/shadow_replay_proof.c) AND by replay_verify_service.c (a legitimate-looking standalone replay verifier) and test_block_log_legacy.c. Do NOT delete the legacy block-log adapters in SWEEP 1; re-evaluate once the importer sweep lands (replay_verify_service may itself be apparatus or may need rewiring to the event_log block source).\n- tools/mcp/controllers/chain_controller.c h_zcl_utxo_projection_diff + zcl_utxo_projection_diff tool (720-, 1052): a two-engine utxo comparison — same class, recommend purging with the *_projection_diff family (flagged in strip_symbols).\n- utxo_projection_set_author / utxo_author_t LEGACY/STAGE flip: the RPC-driven flip dies with cutover_controller.c; the flag itself + its LEGACY branch in utxo_projection.c is the parent-task one-write-path purge, not SWEEP 1.\n\nLint baselines: only one apparatus line found across all baselines — tools/scripts/one_result_type_baseline.txt line 23 (app/services/src/cutover_modes.c) SHRINKS when that file is deleted. The other baselines (one_write_path_baseline.txt etc.) carry the legacy WRITE-path surfaces, which belong to the parent one-write-path purge, not SWEEP 1.\n\nBuild-system: Makefile build rules for tools/shadow_replay_proof.c (and the mutator/ + apparatus objects if explicitly listed) must be pruned; the source tree appears glob-built but the standalone tool target is explicit — verify and SHRINK.\n\nTest registries: lib/test/src/test.c and lib/test/src/test_parallel.c register every deleted test_* (and reference diff_with_legacy_shadow / shadow_replay_proof / conservation_diff_job symbols) — strip those registration lines."
      },
      {
        "area": "SWEEP 2 — legacy importers + always-on mirror (cold-start + live-sync paths)",
        "delete_files": [
          {
            "file": "app/controllers/src/sync_controller_import.c",
            "reason": "PURE LEGACY-ENGINE BRIDGE (995 LOC). Its sole public export node_db_sync_import_utxos() (defined :667; decl app/controllers/include/controllers/sync_controller.h:206) reads a legacy zclassicd LevelDB chainstate (struct coins_view_db) and bulk-INSERTs rows into the legacy SQLite `utxos` table via a direct reader->ring->decoder->writer pipeline (writer binds at :575-602). That `utxos` table IS the legacy coins.db authority that utxo_projection.c's LEGACY branch reads. In the single-engine end state the utxo read-model is built by folding event_log into utxo_projection (lib/storage/src/utxo_projection.c) by the utxo_apply Job — never by importing an external LevelDB. Deleting requires rewiring its 5 callers (see rewire[]).",
            "referenced_by": [
              "main.c:1274 (--importchainstate subcommand)",
              "app/services/src/utxo_recovery_restore.c:126,129 (utxo_recovery_import_ldb fast cold-start)",
              "app/controllers/src/sync_controller_catchup.c:968 (node_db_sync_import_job worker)",
              "app/controllers/src/blockchain_controller_admin.c:285 (importutxos RPC)",
              "app/controllers/include/controllers/sync_controller.h:206 (decl)"
            ]
          },
          {
            "file": "app/controllers/src/legacy_import.c",
            "reason": "LEGACY block-file bridge (1105 LOC). legacy_import() (:688) mmap-scans a legacy zclassicd's blk*.dat files for wallet P2PKH/P2SH outputs + Sapling notes and writes db_wallet_tx rows (:882) / wallet_utxos (DELETE+rebuild at :835) / node_db_sync_wallet_keys (:1062) directly into SQLite. This is a wallet-recovery-from-foreign-node path, not the chain engine; under single-engine the wallet read-model is the wallet_projection folded from event_log + on-chain rescan (replaywalletfromchain / rescanblockchain). Pure-legacy: it parses an EXTERNAL node's files. If owner wants to retain a one-shot 'recover wallet from a legacy datadir' convenience, demote to KEEP-REWIRE (point wallet_view_legacy_import + wallet_diagnostic_repair at a projection-backed rescan) instead of deleting; otherwise delete and drop the 2 RPC callers.",
            "referenced_by": [
              "app/controllers/src/snapshot_controller_import.c:477",
              "app/controllers/src/wallet_diagnostic_repair.c:205",
              "app/views/src/wallet_view.c:241 (wallet_view_legacy_import)",
              "app/controllers/include/controllers/legacy_import.h:20 (decl)",
              "config/src/boot.c:79 (#include only)"
            ]
          }
        ],
        "strip_symbols": [
          {
            "file": "app/controllers/src/sync_controller_catchup.c",
            "symbol": "node_db_sync_import_job_init/_start/_join/_is_started (:972,:980,:999,:1016) + struct node_db_sync_import_job + the worker at :968 calling node_db_sync_import_utxos",
            "what": "STRIP the import-job half (it only drives the deleted node_db_sync_import_utxos LevelDB->utxos bulk load). KEEP node_db_sync_catchup() (:499), sapling_tree_rebuild() (:266), node_db_sync_wallet_keys() (:1122), mempool save/load (:1199,:1247), and node_db_sync_catchup_job_* (:900-954) — those are read-model/recovery primitives, not legacy-engine writes. node_db_sync_catchup writes only the lean block header+txid index into SQLite `blocks` (sync_block_lean :80), which is the same materialization block_index_projection.c owns; see rewire to fold it into the single-engine projection rather than a bespoke catchup."
          },
          {
            "file": "app/services/src/utxo_recovery_restore.c",
            "symbol": "utxo_recovery_import_ldb() (:52) — the node_db_sync_import_utxos calls at :126 and :129",
            "what": "STRIP the LevelDB->SQLite-utxos migration body (legacy coins.db ingest: LOCK-safe copy of ~/.zclassic/chainstate at :100, utxo_recovery_wipe + coins_view_db_open + node_db_sync_import_utxos at :119-129, then derive ldb_height/anchor). KEEP utxo_recovery_restore_chain_tip() (:372). The fast cold-start CAPABILITY this provides must be RE-EXPRESSED (see rewire) as fast_sync (SHA3 UTXO snapshot) + utxo_projection fold, not an import of an external node's LevelDB."
          },
          {
            "file": "config/src/boot.c",
            "symbol": "the three no_legacy_auto_import-guarded blocks: zclassicd-LevelDB block-index import (:1892), block-file hard-link from ~/.zclassic/blocks (:2117), and utxo_recovery_import_ldb call (:2292-2309)",
            "what": "STRIP the always-on legacy auto-import from boot. These are the de-facto -cold-import/-legacy-attach behaviour wired unconditionally at boot, gated only by ctx->no_legacy_auto_import (the -nolegacyimport flag, main.c:1660). Under single-engine, cold-start = fast_sync(FlyClient+SHA3 snapshot) -> event_log seed -> projection rebuild; boot must NOT reach into a sibling zclassicd datadir. KEEP boot_resolve_cold_import_pending_anchor() (:799) ONLY if the anchor-seed semantics are retargeted to the single engine; otherwise it becomes orphaned once import is gone (it consumes cold_import_pending_* metadata written by the deleted importer)."
          },
          {
            "file": "main.c",
            "symbol": "-nolegacyimport flag parse (:1660 ctx.no_legacy_auto_import) and the --importchainstate subcommand (:1245-~1300, calls node_db_sync_import_utxos at :1274) and --importblockindex (:1393, snapshot_import_block_index :1448)",
            "what": "STRIP --importchainstate entirely (it is the CLI front-door to the deleted legacy LevelDB->utxos importer). Once boot's auto-import block is removed, -nolegacyimport has nothing left to disable -> remove the flag and ctx.no_legacy_auto_import field. --importblockindex imports a header index from a RUNNING zclassicd and is a fast-sync source; KEEP only if retargeted to seed event_log/block_index_projection — otherwise it is also a legacy bridge and should go with the importer."
          },
          {
            "file": "app/services/src/legacy_mirror_sync_service.c",
            "symbol": "the external-zclassicd RPC comparison surface: lms_rpc_call/lms_fetch_chain_info/lms_fetch_hash (:255,:271,:291), lms_verify_anchor/lms_verify_after_tip, the whole legacy_mirror_sync_request_catchup() (:571) lag-vs-zclassicd evaluator",
            "what": "KEEP THE FILE but understand its true status: post-B7 it is MONITOR-ONLY (explicit comment :664-668 'the mirror no longer applies blocks; it observes the lag... and lets the native pipeline advance the tip'). It is NOT an importer — it writes no chain state. It IS, however, a comparison-against-an-external-node apparatus (it RPCs a co-located zclassicd to compute lag + hash agreement + emit EV_LAG_SLO_BREACH). Per the B8 plan it is classified KEEP (mirror health monitor backs zcl_mirror_status / getmirrorstatus). DECISION NEEDED: in a strict single-engine end state with no sibling zclassicd, this external probe is dead weight — keep as an optional health monitor (disabled when no zclassicd creds, cfg.enabled=false default already) but do not treat it as part of the sync engine. STRIP nothing structurally; just stop any callers from treating it as authoritative sync."
          }
        ],
        "rewire": [
          {
            "file": "main.c",
            "from": "--importchainstate subcommand at :1245-~1300 (node_db_sync_import_utxos at :1274 + manual wallet_utxos/addresses rebuild)",
            "to": "DELETE the subcommand; cold UTXO state comes from fast_sync (SHA3 UTXO snapshot -> event_log -> utxo_projection fold). No external-LevelDB import front door in the single-engine build."
          },
          {
            "file": "app/services/src/utxo_recovery_restore.c",
            "from": "utxo_recovery_import_ldb() LevelDB->SQLite-utxos migration (:52, body :100-129)",
            "to": "fast_sync snapshot apply + utxo_projection rebuild from event_log. The boot caller (config/src/boot.c:2292 utxo_recovery_import_ldb) should invoke the single-engine cold-start (FlyClient/SHA3 snapshot -> seed event_log -> tip_finalize cursor) instead of importing ~/.zclassic/chainstate."
          },
          {
            "file": "config/src/boot.c",
            "from": "no_legacy_auto_import blocks at :1892 (zclassicd LevelDB block-index import), :2117 (hard-link ~/.zclassic/blocks), :2292 (utxo_recovery_import_ldb)",
            "to": "single-engine cold-start: header_admit->validate_headers->...->utxo_apply->tip_finalize fed by fast_sync/P2P, with block_index_projection (lib/storage/src/block_index_projection.c) as the block-index read-model. Boot must not read a sibling zclassicd datadir."
          },
          {
            "file": "app/controllers/src/sync_controller_catchup.c",
            "from": "node_db_sync_catchup() (:499) lean-index materialization into SQLite `blocks` via sync_block_lean (:80), driven as catchup_job (boot_services.c:1093 boot_start_catchup_service, :1099 job_start)",
            "to": "block_index_projection fold over event_log (lib/storage/src/block_index_projection.c). The catchup job becomes a projection-rebuild trigger rather than a bespoke SQLite block indexer; the boot_services catchup_job wiring (boot_services.c:1096,1099,1585,2174) should drive the projection rebuild instead."
          },
          {
            "file": "app/controllers/src/blockchain_controller_admin.c",
            "from": "importutxos RPC at :285 calling node_db_sync_import_utxos",
            "to": "DELETE or retarget the RPC to fast_sync snapshot apply (single-engine); no operator-facing path may bulk-load the legacy `utxos` table from an external LevelDB."
          },
          {
            "file": "app/controllers/src/wallet_diagnostic_repair.c",
            "from": "legacy_import(legacy_dir,...) at :205 (and app/controllers/src/snapshot_controller_import.c:477, app/views/src/wallet_view.c:241)",
            "to": "wallet_projection fold + on-chain rescan (replaywalletfromchain / rescanblockchain over the single-engine block_index). If wallet-from-foreign-datadir recovery is retained, it must scan via the projection-backed block store, not a parallel mmap of an external node's blk*.dat."
          }
        ],
        "notes": "FLAG STATUS (read main.c argv parser :1615-1669): -legacy-attach, -cold-import, -bodypull are NOT present as CLI string tokens anywhere in the non-worktree tree (grep for the quoted tokens returns nothing). They survive only as (a) the -nolegacyimport flag (main.c:1660 -> ctx.no_legacy_auto_import) which gates the boot auto-import; (b) progress.kv key 'legacy_attach_tip_height/_hash' read by lib/validation/src/accept_block_header.c:181 + the cold-import fast-forward in app/jobs/src/validate_headers_validator.c:269; (c) the g_body_pull signal in lib/validation/src/process_block.c:80 (decl process_block.h:70-96) used by connect_tip.c:694,777 to relax checks during bulk ingest; (d) the --importchainstate / --importblockindex subcommands (main.c:1245,1393). So 'rip out the flags' = (1) remove the -nolegacyimport flag + ctx.no_legacy_auto_import once the boot auto-import blocks are stripped; (2) remove the g_body_pull / process_block.h:70-96 fast-sync relax-flag once cold-start no longer body-pulls (that flag exists to let the legacy bulk-import path skip per-block checks); (3) drop the legacy_attach_tip_* progress keys + their accept_block_header/validate_headers_validator readers when the cold-import anchor mechanism is replaced by the fast_sync cursor stamp.\n\nSHRINK-BASELINE: tools/scripts/one_write_path_baseline.txt does NOT list any of the four SWEEP-2 files directly — it tracks the connect_tip/disconnect_tip/coins_view_cache_flush coins.db WRITE surfaces (those are SWEEP-1 / B8 chain-engine territory, not the importers). So deleting sync_controller_import.c etc. is baseline-neutral for one_write_path_baseline.txt. (Verified: grep of that file shows blockchain_controller_*, chain_state_repository.c, chain_tip.c, activate_best_chain.c, connect_tip.c, etc., none of the SWEEP-2 files.)\n\nLEGACY_MIRROR IS NOT AN IMPORTER (key correction to the brief's framing): legacy_mirror_sync_service.c is post-B7 monitor-only (file comment :664-668). It applies no blocks and writes no chain state. It is a lag/health probe against an external zclassicd (RPC at :255-299) feeding zcl_mirror_status + EV_LAG_SLO_BREACH. The B8 deletion plan (docs/work/b8-comparison-apparatus-deletion-plan.md:357-358) explicitly classifies it and getmirrorstatus as KEEP. Under a STRICT single-engine end state with no co-located zclassicd it is optional dead weight (cfg.enabled defaults false without creds) but is not part of the sync engine and need not be deleted to reach coherence — recommend KEEP as an opt-in external-peer health monitor.\n\nTHE REAL SWEEP-2 SURGERY: the load-bearing legacy importer is the node_db_sync_import_utxos pipeline in sync_controller_import.c (LevelDB chainstate -> legacy SQLite `utxos` authority) plus its always-on boot driver utxo_recovery_import_ldb (utxo_recovery_restore.c:52) wired at config/src/boot.c:2292. THAT is the cold-start capability to re-express against fast_sync + utxo_projection. node_db_sync_catchup (lean block-index) is a SQLite block-index materialization that duplicates block_index_projection -> fold it in. legacy_import.c (wallet-from-foreign-blockfiles) is a separable wallet-recovery convenience -> delete or demote to a projection-backed rescan. The build will be non-building intermediate after deleting sync_controller_import.c until the 5 callers (main.c:1274, utxo_recovery_restore.c:126/129, sync_controller_catchup.c:968, blockchain_controller_admin.c:285) are rewired/stripped per rewire[]."
      },
      {
        "area": "SWEEP 3 — the legacy coins.db WRITE engine (rip out the LEGACY/STAGE duality so the Wave-S Job reducer is the only UTXO writer)",
        "delete_files": [],
        "strip_symbols": [
          {
            "file": "lib/storage/src/utxo_projection.c",
            "symbol": "g_author + utxo_projection_set_author/get_author (lines 85-102)",
            "what": "DELETE the `static _Atomic int g_author = UTXO_AUTHOR_LEGACY;` declaration (line 91) and both accessor functions `utxo_projection_set_author` (93-96) and `utxo_projection_get_author` (98-102). The single-writer authority is no longer a runtime flag — the stage IS the writer unconditionally. Every remaining caller of get_author (see rewires) must be rewired to behave as if STAGE were always set. Keep everything else in this file (the projection open/catch_up/seed/reads/commitment/emit are the surviving single engine)."
          },
          {
            "file": "lib/storage/include/storage/utxo_projection.h",
            "symbol": "utxo_author_t enum + set_author/get_author decls (lines 34-51)",
            "what": "DELETE the `typedef enum {UTXO_AUTHOR_LEGACY, UTXO_AUTHOR_STAGE} utxo_author_t;` block (45-48) and the two decls `void utxo_projection_set_author(utxo_author_t)` / `utxo_author_t utxo_projection_get_author(void)` (50-51), plus the B3 doc comment 34-44. The type and the flag disappear entirely once both branches collapse."
          },
          {
            "file": "lib/validation/src/update_coins.c",
            "symbol": "update_coins_emit_utxo_add_shadow / update_coins_emit_utxo_spend_shadow (lines 44-88) + their callsites (145, 187)",
            "what": "These two legacy emitters exist ONLY to author EV_UTXO_ADD/SPEND while g_author==LEGACY (they early-return when author != LEGACY, lines 53-54 / 77-78). In the single-engine world the STAGE (utxo_apply_stage.emit_delta) is the sole author, so these legacy emitters are dead. DELETE both functions, their decls in utxo_projection.h (129-139 are the shared emit primitives — KEEP those; the *_shadow wrappers are local to update_coins.c so just delete them here), the g_utxo_event_emit_total/fail counters (30-42), and remove the two call sites in update_coins_with_undo (line 145 emit_spend, line 187 emit_add). update_coins itself STAYS as the legacy coins.db cache mutator until connect_tip is rewired off coins.db (see notes/ordering); only the event-emission half is stripped here."
          },
          {
            "file": "lib/validation/src/connect_block.c",
            "symbol": "update_coins_emit_utxo_add_shadow (line 752) / update_coins_emit_utxo_spend_shadow (line 799) in disconnect_block",
            "what": "disconnect_block calls the same two legacy shadow emitters on the rollback side (add at 752, spend at 799). These are the disconnect-side legacy authorship and are made redundant by utxo_apply_stage's reorg-unwind inverse-delta path (utxo_apply_reorg_unwind_if_needed). DELETE both call sites. The coins-cache mutation in disconnect_block stays until the cache itself is retired (notes)."
          },
          {
            "file": "lib/storage/src/coins_view_stage_backing.c",
            "symbol": "coins_view_select_connect_backing — the `if (utxo_projection_get_author() != UTXO_AUTHOR_STAGE) {*out=*legacy; return true;}` LEGACY branch (lines 91-94)",
            "what": "STRIP the author-gated LEGACY early-return so the composite (projection-backed reads) is selected UNCONDITIONALLY whenever a projection handle exists. After the strip the function only does: if (!proj||!sb) fall back+log; else build the composite. This makes connect_block's input lookups source from the UTXO projection always (the brief's 'where connect_block reads must source from the utxo projection'). Keep the NULL-proj defensive fallback (102-109) and the composite build (111-122)."
          },
          {
            "file": "app/jobs/src/utxo_apply_stage.c",
            "symbol": "emit_delta gate `if (summary.ok && utxo_projection_get_author() == UTXO_AUTHOR_STAGE)` (line 260)",
            "what": "STRIP the get_author()==STAGE condition so emit_delta runs unconditionally on every verified block (it becomes `if (summary.ok) emit_delta(...)`). This is what makes the Wave-S Job the sole, unconditional UTXO writer. Update the stale 'no-op until B7' comment 256-259 and the 'shadow mode' log string at line 358."
          },
          {
            "file": "app/jobs/src/utxo_apply_delta.c",
            "symbol": "reorg-unwind author gate `if (utxo_projection_get_author() == UTXO_AUTHOR_STAGE)` (line 730)",
            "what": "STRIP the get_author()==STAGE guard around the stage-side inverse-delta emit so the disconnect/reorg-unwind emits EV_UTXO_SPEND/ADD unconditionally (the disconnect-side counterpart to emit_delta). This pairs with deleting the disconnect_block legacy emitters above so there is exactly one writer on the rollback path too."
          },
          {
            "file": "app/controllers/src/cutover_controller.c",
            "symbol": "utxo_projection_set_author call in the stage=all branch (lines 267-281)",
            "what": "DELETE the `utxo_projection_set_author(... ? UTXO_AUTHOR_STAGE : UTXO_AUTHOR_LEGACY)` call and its B5-keystone comment. The author flip vanishes with the flag. (The wider cutover_controller flip RPC is SWEEP-1/apparatus territory; for SWEEP 3 only this one set_author line is in scope — it is the sole production caller of set_author.)"
          },
          {
            "file": "lib/validation/src/connect_tip.c",
            "symbol": "coins_view_cache_flush(&view) (line 523) + flush_coins_if_needed(coins_tip,...) (lines 548, 718, 968) + the per-block coins.db commit + SHA3 checkpoint block (539-587) + block_index/coins ordering",
            "what": "This is the core legacy coins.db WRITE in the hot path. In the single-engine end state the durable UTXO set is the projection (folded from the event log by utxo_apply_stage), NOT coins.db. STRIP the coins.db write half: the in-RAM `view` flush to coins_tip at 523 stays only as long as the RAM read-cache is kept warm, but the durable `flush_coins_if_needed(coins_tip, ...)` calls at 548/718/968 (which call coins_view_sqlite_batch_write_ex) must be removed once utxo_apply_stage durably owns the UTXO set. The SHA3-checkpoint block (544-587) reads the coins.db `utxos` table via utxo_commitment_sha3_compute(ndb->db,...) — repoint it to utxo_projection_commitment(). NOTE: this is the riskiest strip; see notes for the mandatory ordering (it can only happen after the projection is seeded+caught-up and proven, which the brief authorizes as a possibly-non-building intermediate)."
          },
          {
            "file": "lib/validation/src/disconnect_tip.c",
            "symbol": "coins_view_cache_flush(&view) (line 192) + node_db_sync_disconnect_block (208)",
            "what": "Disconnect-side legacy coins.db write. Same treatment as connect_tip: the durable coins.db flush at 192 and the SQLite projection sync at 204-210 are legacy; the authoritative rollback is utxo_apply_stage's inverse-delta. STRIP the coins.db durable writes; the in-memory undo-apply logic and the BIP30 invariant check (240-256) stay as a verification/read concern."
          },
          {
            "file": "lib/validation/src/process_block_flush_policy.c",
            "symbol": "flush_coins_if_needed coins.db write (lines 235-252) + set_coins_sqlite_for_commitment / g_coins_sqlite_ptr (73-84)",
            "what": "flush_coins_if_needed is the legacy coins.db batch-write gate (calls coins_view_sqlite_batch_write_ex at 237). In the single-engine end state this is dead — the projection is the durable UTXO store. STRIP the coins.db branch (236-249), reducing the function to the Sapling-tree checkpoint persistence (which is unrelated and STAYS). Eventually delete g_coins_sqlite_ptr + set_coins_sqlite_for_commitment once no caller needs the coins.db handle for commitment."
          },
          {
            "file": "lib/storage/src/coins_view_sqlite.c",
            "symbol": "the whole file is the legacy coins.db UTXO authority (batch_write_ex at 696, the utxos INSERT/DELETE stmts 491-502, the cvs_vtable 58-64, coins_rewind_above_tip 139, check_tip_consistency 280)",
            "what": "This file is the legacy coins.db WRITE engine. In the END STATE it should be DELETED (it is the second UTXO writer the brief wants gone). For SWEEP 3 the in-file action is: it survives only as long as connect_tip/disconnect_tip/flush_coins_if_needed still flush to it; once those strips above land it has no production writer. Mark it for deletion in the same wave as the connect_tip/disconnect_tip coins.db strips. (Read primitives coins_view_sqlite_get_coins/have_coins/get_best_block become redundant once coins_view_select_connect_backing always returns the projection composite.) Until deleted, the cvs_batch_write_impl vtable thunk (51-56) is the legacy writer behind coins_view_cache_flush."
          },
          {
            "file": "app/services/src/chain_state_repository.c",
            "symbol": "coins_tip coupling: csr->coins_tip field + coins_view_cache_set_best_block calls (lines 631, 658, 710) and csr_persist_coins_best_locked (151-203) writing node_state 'coins_best_block'",
            "what": "chain_state_repository STAYS (it is the in-memory tip + commit-validation guard — part of the single engine's tip bookkeeping). But its coins.db coupling is legacy duality: persisting `coins_best_block` (the coins.db anchor) and mutating coins_tip's best_block. In the end state the definitional tip is tip_finalize_stage's progress.kv cursor, not coins_best_block. STRIP the coins_best_block persistence (csr_persist_coins_best_locked + the persist_coins_best path in csr_commit_tip 686-695) and the coins_tip->set_best_block calls (631/658/710) once tip_finalize owns the cursor. Keep all the validation guards (csr_validate_locked) and active_chain_set_tip — those are the surviving in-memory tip authority."
          },
          {
            "file": "app/services/src/chain_tip.c",
            "symbol": "chain_tip_fsync_barrier coins.db cacheflush (lines 57-84) + active_chain_set_tip (118, 132)",
            "what": "chain_set_active_tip STAYS (in-memory tip publication for the single engine). STRIP the coins.db-specific fsync barrier (chain_tip_fsync_barrier flushes ndb->db, the coins.db page cache, lines 57-84 and its call at 178) — once the projection/event-log is the durable store, the fsync barrier moves to the event_log/progress.kv, not coins.db. active_chain_set_tip stays (it is the RAM tip)."
          }
        ],
        "rewire": [
          {
            "file": "lib/storage/src/coins_view_stage_backing.c",
            "from": "coins_view_select_connect_backing branches on utxo_projection_get_author()",
            "to": "Always build the projection-backed composite view (drop the LEGACY branch); callers (connect_tip line 274-276) get projection-sourced reads unconditionally"
          },
          {
            "file": "lib/validation/src/connect_tip.c",
            "from": "coins_view_select_connect_backing(...) at lines 273-277 still passes utxo_projection_get_global() and depends on the author flag for backing selection",
            "to": "After the flag is gone, connect_block input lookups resolve through the UTXO projection unconditionally (the composite). The coins_tip cache stays only as a RAM read-accelerator; durable UTXO writes are removed (see strip)"
          },
          {
            "file": "lib/validation/src/connect_tip.c",
            "from": "SHA3 UTXO checkpoint reads coins.db: utxo_commitment_sha3_compute(ndb->db, sha3, &count) at line 554",
            "to": "utxo_projection_commitment(utxo_projection_get_global(), sha3) — verify the checkpoint against the projection (the single engine), not the legacy utxos table"
          },
          {
            "file": "lib/validation/include/validation/process_block.h",
            "from": "connect_tip/disconnect_tip/process_new_block signatures take struct coins_view_cache *coins_tip (lines 42-62)",
            "to": "In the end state the coins_tip parameter becomes vestigial (RAM cache only) or is dropped; callers in activate_best_chain.c (lines 204-869) and chain_activation_controller.c (405-437) pass it today — they keep working during the intermediate but the durable-write semantics move to utxo_apply_stage"
          },
          {
            "file": "app/jobs/src/utxo_apply_stage.c",
            "from": "emit_delta gated on get_author()==STAGE (line 260) and reorg path (utxo_apply_delta.c:730) gated likewise — i.e. the stage only writes when the operator flipped authority",
            "to": "emit_delta + reorg inverse-delta run unconditionally on every verified/disconnected block — utxo_apply_stage is the SOLE, always-on UTXO writer feeding utxo_projection"
          },
          {
            "file": "lib/validation/src/process_block_flush_policy.c",
            "from": "flush_coins_if_needed → coins_view_sqlite_batch_write_ex(g_coins_sqlite_ptr, ...) writes the legacy utxos table (line 237)",
            "to": "No coins.db durable write — utxo_apply_stage authors EV_UTXO_* and utxo_projection_catch_up folds them into the projection (the durable UTXO store). Only the Sapling-tree checkpoint persistence remains in this function"
          },
          {
            "file": "app/services/src/chain_state_repository.c",
            "from": "csr persists 'coins_best_block' to node_state and sets coins_tip best-block as the durable tip anchor (csr_persist_coins_best_locked, coins_view_cache_set_best_block)",
            "to": "tip_finalize_stage's progress.kv cursor is the definitional tip; csr keeps only the in-memory active_chain_set_tip publication + its commit-validation guards"
          }
        ],
        "notes": "SCOPE: SWEEP 3 = the legacy coins.db WRITE engine. The 64 baseline surfaces in tools/scripts/one_write_path_baseline.txt are matched by the gate pattern (check_one_write_path.sh line 33): active_chain_set_tip | coins_view_sqlite_batch_write(_ex) | coins_view_cache_flush | utxo_projection_set_author | process_new_block | connect_tip | disconnect_tip. SHRINK-BASELINE: every baseline line whose underlying call is deleted goes away. Specifically removable on completion: line 37 (utxo_projection.h set_author decl), 42 (utxo_projection.c set_author def), 35/36/38/39/40/41 (coins_view_sqlite_batch_write* — the legacy SQLite writer dies), 30/31 (coins_view_cache_flush decl+def, once no durable flush caller remains), 59/61/64/65 (connect_tip/disconnect_tip/flush_policy coins.db flushes), and the dozens of `coins_view_cache_flush(ctx->coins_tip)` admin/repair/boot lines (3-29, 106-203, 311-522) which are read-cache-flush helpers that lose meaning once coins.db is not the store. The active_chain_set_tip lines (15-20, 58) STAY (in-memory tip is the single engine's RAM tip). process_new_block lines (12,32-34,46,63) STAY (block-acceptance entrypoint).\n\nTHE TWO ENGINES, CONCRETELY: (1) LEGACY coins.db writer = update_coins.c shadow emitters [LEGACY-gated] + connect_block/disconnect_block emit calls + connect_tip/disconnect_tip → coins_view_cache_flush → coins_view_sqlite_batch_write_ex → `utxos` table, with coins_view_sqlite.c as authority, driven by activate_best_chain.c. (2) SINGLE engine = event_log → utxo_apply_stage (emit_delta) → utxo_projection.c fold → `utxo` table; tip = tip_finalize progress.kv cursor. The duality knob is g_author (default LEGACY) read in 5 production spots: update_coins.c emitters (53,77), coins_view_stage_backing.c select (91), utxo_apply_stage.c emit_delta (260), utxo_apply_delta.c reorg (730); flipped in exactly one production caller cutover_controller.c (278). Strip the flag → STAGE path is unconditional.\n\nORDERING (the brief permits a non-building intermediate but demands a coherent single-engine END STATE — do NOT leave rubble): The flag-strip (utxo_projection g_author, the 5 read sites, the cutover_controller flip) is the cheap, coherent first cut — it makes the projection the unconditional reader for connect_block AND makes utxo_apply_stage the unconditional UTXO author. The expensive cut is retiring the coins.db DURABLE writes in connect_tip/disconnect_tip/flush_coins_if_needed and deleting coins_view_sqlite.c; that must be sequenced AFTER the projection is anchor-seeded (utxo_projection_seed_from_legacy, already shipped) and proven to track, because until then coins.db is the only populated UTXO store. The SHA3 checkpoint in connect_tip (554) and the boot tip-consistency check in coins_view_sqlite.c (280) are the two consensus-critical reads that must be repointed to utxo_projection_commitment/count BEFORE the coins.db writer is deleted, or the node loses its integrity gate.\n\nDO-NOT-TOUCH (single-engine survivors, not duality): utxo_projection.c open/catch_up/seed_from_legacy/reads/commitment/emit_add/emit_spend; coins_view_projection.c (the projection-backed read view — this is the single engine's read path); utxo_apply_stage.c minus the author gate; tip_finalize_stage.c; csr's active_chain_set_tip + validation guards; chain_set_active_tip's in-memory publication. coins_view_select_connect_backing STAYS as a function but loses its branch (becomes 'always composite').\n\nVERIFICATION ANCHORS: read connect_tip.c:273-277 (backing select), 523/548/718/968 (coins flushes), 544-587 (SHA3 checkpoint); update_coins.c:44-88,145,187 (emitters); utxo_projection.c:85-102 (g_author); coins_view_stage_backing.c:91-94 (the branch); utxo_apply_stage.c:260 (emit gate); utxo_apply_delta.c:730 (reorg gate); cutover_controller.c:278 (the one production flip); coins_view_sqlite.c:696 (batch_write_ex, the legacy authority). All line numbers verified against the working tree this session."
      },
      {
        "area": "SWEEP 4 — boot wiring, MCP/RPC surface, tests, lint baselines for the cutover/shadow/diff/dual-engine apparatus purge",
        "delete_files": [
          {
            "file": "app/jobs/src/conservation_diff_job.c",
            "reason": "The conservation-diff Job exists ONLY to reconcile the fed (shadow) block log against legacy zclassicd byte-for-byte (the fed==diffed safety law of the dual-engine cutover). Reads block_log_legacy + a legacy block_log_port; writes only its own progress.kv cursor. Pure apparatus.",
            "referenced_by": [
              "app/jobs/include/jobs/conservation_diff_job.h (delete header too)",
              "config/src/boot_services.c (include line 25; shutdown call line 3167 — STRIP)",
              "app/supervisors/src/staged_sync_supervisor.c (include line 25; staged_conservation_diff_register + cd_tick/cd_stall lines 414-464; call line 482; CONSERVATION_DIFF_BATCH_PER_TICK — STRIP)",
              "app/controllers/src/diagnostics_registry.c (include line 51; g_dumpers entry lines 447-449 — STRIP)",
              "lib/test/src/test_conservation_diff_job.c (delete)",
              "lib/test/src/test.c (registration lines 816 — STRIP)"
            ]
          },
          {
            "file": "app/services/src/cutover_modes.c",
            "reason": "Holds the runtime shadow|authoritative mode flags (header_admit_mode_t / validate_headers_mode_t / tip_finalize_mode_t) whose only purpose is to gate the LEGACY vs STAGE branch during the soak. After the purge the staged path runs unconditionally, so the mode concept disappears.",
            "referenced_by": [
              "app/services/include/services/cutover_modes.h (delete header)",
              "app/jobs/src/header_admit_stage.c + jobs/header_admit_stage.h (header_admit_get_mode/MODE enum — STRIP, stage runs unconditionally)",
              "app/jobs/src/validate_headers_stage.c + jobs/validate_headers_stage.h (mode gate — STRIP)",
              "app/jobs/src/tip_finalize_stage.c + jobs/tip_finalize_stage.h (mode gate — STRIP)",
              "lib/validation/src/accept_block_header.c (HEADER_ADMIT_MODE enum decl lines 149-157, validate_headers_authoritative_guard + header_admit_get_mode()==AUTHORITATIVE branch lines 193-362 — STRIP; consensus-sweep scope)",
              "app/controllers/src/cutover_controller.c + cutover_controller_preflight.c (delete)",
              "app/conditions/src/cutover_canary_complete.c + cutover_no_forward_progress.c (delete)",
              "app/controllers/src/diagnostics_registry.c (cutover dumper lines 458-463 — STRIP)"
            ]
          },
          {
            "file": "app/controllers/src/cutover_controller.c",
            "reason": "Implements the cutovermode / cutoverpreflight RPCs (diag_rpc_cutovermode, diag_rpc_cutoverpreflight, parse_cutover_mode, push_cutover_modes, cutover_preflight_ready_now) — the entire runtime cutover-flip control surface. 288 LOC, pure apparatus.",
            "referenced_by": [
              "app/controllers/src/cutover_controller_preflight.c (delete together)",
              "app/controllers/src/diagnostics_controller.c (RPC table entries lines 32-33 — STRIP)",
              "app/controllers/include/controllers/diagnostics_internal.h (diag_rpc_cutovermode/diag_rpc_cutoverpreflight + diag_rpc_cutoverpreflight() decl — STRIP)",
              "tools/mcp/controllers/ops_controller.c (h_zcl_cutovermode/h_zcl_cutoverpreflight handlers, p_cutovermode/p_cutoverpreflight specs, k_routes entries — STRIP)"
            ]
          },
          {
            "file": "app/controllers/src/cutover_controller_preflight.c",
            "reason": "718-LOC implementation of the cutoverpreflight go/no-go gate that ANDs UTXO-commitment parity, header/validate cursor lag, and shadow conservation against the legacy engine. Exists only to certify the dual-engine flip.",
            "referenced_by": [
              "app/controllers/src/cutover_controller.c (calls diag_rpc_cutoverpreflight — deleted together)",
              "lib/test/src/test_cutover_preflight.c (delete)"
            ]
          },
          {
            "file": "app/controllers/src/projection_diff_controller.c",
            "reason": "715-LOC controller whose every handler (diag_rpc_getmirrorstatus + diag_rpc_{peers,mempool,znam,wallet,contacts,onionannouncements,hodlhistory}projectiondiff) folds a projection over the event_log and compares it byte-for-byte to the legacy SQLite table it shadows. This IS the *_projection_diff comparison apparatus named in the brief. The projections themselves stay; this side-by-side comparator goes.",
            "referenced_by": [
              "app/controllers/src/diagnostics_controller.c (RPC table entries lines 37-47 getmirrorstatus + 7 projectiondiff — STRIP)",
              "app/controllers/include/controllers/diagnostics_internal.h (the diag_rpc_*projectiondiff + diag_rpc_getmirrorstatus decls — STRIP)",
              "tools/mcp/controllers/diagnostics_controller.c (DEFINE_PT lines 128-134 + k_routes lines 629-658 + diag_with_legacy line 667-673 — STRIP)",
              "tools/mcp/controllers/ops_controller.c (mirror_status surface)"
            ]
          },
          {
            "file": "app/conditions/src/cutover_canary_complete.c",
            "reason": "Auto-revert Condition that watches for divergence after the authoritative flip and reverts to shadow mode. Pure cutover-soak safety net; meaningless once there is one engine.",
            "referenced_by": [
              "app/conditions/src/condition_registry.c (register_cutover_canary_complete decl line 24 + call line 50 — STRIP)",
              "app/services/src/cutover_modes.c (reads mode flags — deleted together)"
            ]
          },
          {
            "file": "app/conditions/src/cutover_no_forward_progress.c",
            "reason": "Auto-revert Condition that flips back to shadow if the authoritative tip stops advancing during the soak. Apparatus that only exists to guard the cutover transition.",
            "referenced_by": [
              "app/conditions/src/condition_registry.c (register_cutover_no_forward_progress decl line 23 + call line 49 — STRIP)",
              "lib/test/src/test_cutover_autorevert.c (delete)"
            ]
          },
          {
            "file": "application/operations/src/diff_with_legacy_shadow.c",
            "reason": "Use case that takes two block_log_port views (legacy vs in-process shadow) and reports the first divergence. The core of the zcl_diff_with_legacy_shadow tool. Dual-engine comparator.",
            "referenced_by": [
              "application/operations/include/application/operations/diff_with_legacy_shadow.h (delete header)",
              "tools/mcp/controllers/chain_controller.c (include line 16; h_zcl_diff_with_legacy_shadow handler + diff_status_name + p_diff_shadow + k_route line 1027-1032 — STRIP)",
              "lib/test/src/test_diff_with_legacy_shadow.c (delete)"
            ]
          },
          {
            "file": "application/operations/src/shadow_replay_proof.c",
            "reason": "Operations-layer driver that replays the legacy archive 0->tip feeding the shadow pipeline and proving blocks_fed==blocks_diffed. The cutover PROVE-phase engine. No role after single-engine.",
            "referenced_by": [
              "application/operations/include/application/operations/shadow_replay_proof.h (delete header)",
              "tools/shadow_replay_proof.c (delete CLI driver)",
              "lib/test/src/test_shadow_replay_proof.c + test_shadow_replay_full_driver.c (delete)",
              "Makefile (BUILD_NODE_TOOL shadow_replay_proof line 169 — STRIP)"
            ]
          },
          {
            "file": "tools/shadow_replay_proof.c",
            "reason": "Standalone CLI binary that runs the full-archive shadow replay proof against a read-only legacy datadir. Built via Makefile line 169. Dual-engine PROVE tool.",
            "referenced_by": [
              "Makefile (line 169 $(eval $(call BUILD_NODE_TOOL,shadow_replay_proof,tools/shadow_replay_proof.c)) — STRIP)",
              "application/operations/src/shadow_replay_proof.c (deleted together)"
            ]
          },
          {
            "file": "app/services/src/replay_verify_service.c",
            "reason": "Offline integrity/PoW sweep over the LEGACY on-disk block log (block_log_legacy/block_log_file), the engine behind zcl_replay_verify. It only verifies the legacy archive that is being purged; it does not feed the single engine. Sole non-apparatus consumer of block_log_legacy besides the deleted conservation_diff_job.",
            "referenced_by": [
              "app/services/include/services/replay_verify_service.h (delete header)",
              "tools/mcp/controllers/chain_controller.c (include line 25; h_zcl_replay_verify handler + p_replay_verify + k_route line 1040-1051 — STRIP)",
              "lib/test/src/test_replay_verify.c (delete)",
              "lib/test/src/test.c (registration line 848 — STRIP)"
            ]
          },
          {
            "file": "adapters/outbound/persistence/block_log_legacy.c",
            "reason": "Reader for the legacy zclassicd on-disk blkNNNNN.dat archive. After conservation_diff_job and replay_verify_service are deleted there are NO non-test consumers (confirmed: grep shows only those two .c files). Pure legacy-archive bridge.",
            "referenced_by": [
              "adapters/outbound/persistence/block_log_legacy.h (delete header)",
              "app/jobs/src/conservation_diff_job.c (deleted)",
              "app/services/src/replay_verify_service.c (deleted)",
              "tools/shadow_replay_proof.c (deleted)",
              "lib/test/src/test_block_log_legacy.c (delete; registered test.c line 847 — STRIP)"
            ]
          },
          {
            "file": "adapters/outbound/persistence/block_log_file.c",
            "reason": "The block_log_port-backed file reader used as the two comparison sides. Confirmed non-test consumers are ONLY tools/shadow_replay_proof.c, chain_controller.c (diff/replay handlers), replay_verify_service.c, conservation_diff_job.c — all deleted/stripped. VERIFY no staged body source uses it before deleting (it did not appear in the body_fetch/body_persist source set); if a staged source does consume it, downgrade to keep.",
            "referenced_by": [
              "adapters/outbound/persistence/block_log_file.h (delete header if unused)",
              "tools/mcp/controllers/chain_controller.c (include line 14 — STRIP)",
              "lib/test/src/test_block_log_file.c (delete; registered test.c line 846 — STRIP)"
            ]
          },
          {
            "file": "mutator/src/mutator.c",
            "reason": "The mutator + bounded input-queue is the worker thread the shadow_feeder pushes legacy-accepted blocks into. Only consumer is boot_shadow_feeder_start (-shadow). Once the shadow feeder is gone the mutator has no caller.",
            "referenced_by": [
              "mutator/src/input_queue.c + mutator/src/cmd.c + mutator/include/* (delete whole mutator/ tree)",
              "config/src/boot_services.c (include line 161; g_shadow_mutator + mutator_start/mutator_stop in boot_shadow_feeder_* lines 162-237 — STRIP)",
              "Makefile (MUTATOR_SRCS line 51 — SHRINK/STRIP)",
              "lib/test/src/test_mutator.c (delete; registered test.c line 849 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_flip_dryrun.c",
            "reason": "End-to-end exercise of the full shadow->authoritative cutovermode flip on an ephemeral datadir. Tests the apparatus being deleted.",
            "referenced_by": [
              "lib/test/src/test.c (registration line 857 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_postflip_reorg.c",
            "reason": "Tests a reorg AFTER the cutover flip when tip_finalize_log is definitional. Cutover-transition scenario; gone with the duality.",
            "referenced_by": [
              "lib/test/src/test.c (registration line 858 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_tip_parity.c",
            "reason": "Proves log-derived tip == legacy chain_active tip block-by-block before the flip. Dual-engine parity proof; obsolete once log_head IS the tip.",
            "referenced_by": [
              "lib/test/src/test.c (registration line 856 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_autorevert.c",
            "reason": "Proves the cutover auto-revert Conditions fire. Tests cutover_canary_complete.c / cutover_no_forward_progress.c which are deleted.",
            "referenced_by": [
              "lib/test/src/test.c (registration line 859 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_cutover_preflight.c",
            "reason": "B8 live-preflight UTXO-commitment parity gate test for cutover_controller_preflight.c.",
            "referenced_by": [
              "lib/test/src/test.c (registration line 860 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_shadow_conservation.c",
            "reason": "Tests adapters/inbound/shadow_conservation.c (fed/diffed ledger) — the conservation accounting of the dual engine.",
            "referenced_by": [
              "lib/test/src/test.c (registration line 852 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_shadow_feeder.c",
            "reason": "Tests adapters/inbound/shadow_feeder.c — the legacy->shadow mirroring hook deleted in sweeps 1-3.",
            "referenced_by": [
              "lib/test/src/test.c (registration line 850 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_shadow_feeder_global.c",
            "reason": "Tests adapters/inbound/shadow_feeder_global.c global hook installer.",
            "referenced_by": [
              "lib/test/src/test.c (registration line 851 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_parity_diff_gate.c",
            "reason": "MVP-criterion-8 CI gate that pins the local-vs-remote-zclassicd parity comparison contract (height + UTXO match against the legacy peer). Dual-engine parity gate; also has a ZCL_TEST_ONLY=parity_diff subset path in test.c lines 99-102.",
            "referenced_by": [
              "lib/test/src/test.c (ZCL_TEST_ONLY subset lines 99-102 + full registration line 661 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_reorg_parity.c",
            "reason": "'Reorg parity proof — the cutover keystone' (docs/work/cutover.md reorg corpus). Proves a reorg yields byte-identical UTXO state across paths — a cutover PROVE artifact. VERIFY it does not also serve as the general reorg-correctness proof for the single engine before deleting; if it asserts pure-projection reorg behavior without a legacy side, downgrade to keep.",
            "referenced_by": [
              "lib/test/src/test.c (registration line 736 — STRIP)"
            ]
          },
          {
            "file": "lib/test/src/test_reorg_projection_parity.c",
            "reason": "Proves the LEGACY disconnect path (disconnect_block / update_coins_with_undo) emits shadow UTXO events correctly so the shadow projection matches. Tied to the legacy emitter being deleted. The forward-only projection reorg behavior is covered by test_stage_reorg_unwind_parity (keep that).",
            "referenced_by": [
              "lib/test/src/test.c (registration line 737 — STRIP)"
            ]
          }
        ],
        "strip_symbols": [
          {
            "file": "main.c",
            "symbol": "-shadow arg parse (line 1659)",
            "what": "DELETE the line `else if (strcmp(argv[i], \"-shadow\") == 0) ctx.shadow_feeder_enabled = true;`. The -shadow CLI flag is the sole entry to the shadow feeder. Everything else in main.c stays."
          },
          {
            "file": "config/include/config/boot.h",
            "symbol": "struct app_context field shadow_feeder_enabled (lines 54-58)",
            "what": "DELETE the `bool shadow_feeder_enabled;` field and its -shadow comment. No struct field for the dead flag."
          },
          {
            "file": "config/src/boot_services.c",
            "symbol": "shadow_feeder boot block (lines 157-237)",
            "what": "DELETE the includes (shadow_feeder.h, shadow_feeder_global.h, mutator/mutator.h lines 159-161), the g_shadow_mutator/g_shadow_feeder statics (162-163), and boot_shadow_feeder_start/_disarm/_dispose (165-237). Then STRIP their call sites: boot_shadow_feeder_start(ctx) line 3016, boot_shadow_feeder_disarm() line 3241, boot_shadow_feeder_dispose() line 3254."
          },
          {
            "file": "config/src/boot_services.c",
            "symbol": "conservation_diff_job wiring",
            "what": "DELETE include `jobs/conservation_diff_job.h` (line 25) and the `conservation_diff_job_shutdown();` call + its comment block (lines 3163-3167)."
          },
          {
            "file": "config/src/boot_services.c",
            "symbol": "boot_start_phase4_storage_shadow (lines 1104-end of function ~1290) + call site line 2212",
            "what": "KEEP the function (it opens the event_log + all 8 projections = the engine that stays) but RENAME/RE-COMMENT off 'shadow' (it is now the unconditional storage engine, not a shadow). STRIP the legacy-seed step: the `utxo_projection_seed_from_legacy(... seed_ndb->db)` block (lines 1196-1218) reads from the legacy coins.db — once coins.db is gone this anchor-seed has no source; remove it. STRIP all `// obs-ok:phase4-shadow` markers and 'shadow emit only' / 'until cutover' wording. The function must run unconditionally (it already does — called at line 2212 with no flag guard)."
          },
          {
            "file": "app/supervisors/src/staged_sync_supervisor.c",
            "symbol": "conservation_diff Job child (include line 25; CONSERVATION_DIFF_BATCH_PER_TICK macro; cd_tick/cd_stall lines 418-437; staged_conservation_diff_register lines 439-464; call line 482)",
            "what": "DELETE the conservation_diff include, the batch macro, cd_tick, cd_stall, staged_conservation_diff_register, and the staged_conservation_diff_register(datadir) call at line 482. The 8 reducer-stage registrations (header_admit..tip_finalize, lines 474-481) STAY — they ARE the engine. Optionally retire the now-unused `datadir` param of staged_sync_supervisor_register, and rename SHADOW_STAGE_QUIET_US -> STAGE_QUIET_US / drop 'shadow' from the stall-log strings (cosmetic)."
          },
          {
            "file": "app/controllers/src/diagnostics_controller.c",
            "symbol": "register_diagnostics_rpc_commands cmds[] (lines 30-48)",
            "what": "DELETE the rows: cutovermode (32), cutoverpreflight (33), getmirrorstatus (37), peersprojectiondiff (38), mempoolprojectiondiff (39), znamprojectiondiff (40), walletprojectiondiff (41), contactsprojectiondiff (42-43), onionannouncementsprojectiondiff (44-45), hodlhistoryprojectiondiff (46-47). KEEP dumpstate, getnodelog, dbquery, probezclassicd. Update the header comment block (lines 12-16) to drop the cutover_controller / projection_diff_controller bullets."
          },
          {
            "file": "app/controllers/include/controllers/diagnostics_internal.h",
            "symbol": "diag_rpc_* prototypes",
            "what": "DELETE the decls for diag_rpc_cutovermode, diag_rpc_cutoverpreflight, diag_rpc_getmirrorstatus, diag_rpc_peersprojectiondiff, diag_rpc_mempoolprojectiondiff, diag_rpc_znamprojectiondiff, diag_rpc_walletprojectiondiff, diag_rpc_contactsprojectiondiff, diag_rpc_onionannouncementsprojectiondiff, diag_rpc_hodlhistoryprojectiondiff. KEEP dumpstate/getnodelog/dbquery/probezclassicd + the g_dumpers contract."
          },
          {
            "file": "app/controllers/src/diagnostics_registry.c",
            "symbol": "g_dumpers[] cutover/conservation entries + include",
            "what": "DELETE include `jobs/conservation_diff_job.h` (line 51), the conservation_diff dumper entry (lines 447-449), and the cutover dumper entry (lines 458-463). KEEP the projection dumpers (mempool_projection 476-477, peers_projection 478-479, utxo_projection 480-485, znam_projection 486-489) but EDIT the utxo_projection description (lines 480-485) to remove 'Shadow-mode' / 'diff via zcl_utxo_projection_diff before cutover'. Removing these two dumper rows auto-shrinks the zcl_state enum_csv (it is derived dynamically — see diagnostics_controller.c line 714 g_state_subsystems_csv)."
          },
          {
            "file": "app/conditions/src/condition_registry.c",
            "symbol": "cutover condition registration (decls lines 23-24, calls lines 49-50)",
            "what": "DELETE `void register_cutover_no_forward_progress(void);` (23), `void register_cutover_canary_complete(void);` (24), and the two calls register_cutover_no_forward_progress() (49) + register_cutover_canary_complete() (50). Other Condition registrations stay."
          },
          {
            "file": "tools/mcp/controllers/chain_controller.c",
            "symbol": "diff/cutover/replay MCP handlers + routes",
            "what": "DELETE includes shadow_conservation.h (12), shadow_feeder_global.h (13), block_log_file.h (14), block_log_legacy.h (15), diff_with_legacy_shadow.h (16), ports/block_log_port.h (23), jobs/header_admit_stage.h (24), services/replay_verify_service.h (25), storage/block_index_projection.h (26 — only if block_index_diff is the sole user). DELETE handlers diff_status_name (83), h_zcl_diff_with_legacy_shadow (95-~262), h_zcl_diff_staged_header_admit (296-~378), h_zcl_replay_verify (390-459), h_zcl_block_index_diff + live_index_entry/live_entry_cmp/absorb_live_entry (475-~700), h_zcl_utxo_projection_diff (720-~825). DELETE param specs p_diff_shadow (934), p_diff_staged (946), p_replay_verify (956). DELETE k_routes rows zcl_diff_with_legacy_shadow (1027-1032), zcl_diff_staged_header_admit (1033-1039), zcl_replay_verify (1040-1051), zcl_utxo_projection_diff (1052-1060), zcl_block_index_diff (1061-1068). KEEP all real chain tools (getblock, utxocommitment, mmb, syncstate, etc)."
          },
          {
            "file": "tools/mcp/controllers/ops_controller.c",
            "symbol": "cutovermode/cutoverpreflight MCP tools",
            "what": "DELETE h_zcl_cutovermode (295-322), h_zcl_cutoverpreflight (324-348), p_cutovermode (841-847), p_cutoverpreflight (849-...), and k_routes entries zcl_cutovermode (946-953) + zcl_cutoverpreflight (955-960). Leave zcl_mirror_status only if it does not route through the deleted projection_diff_controller (it uses dumpstate of legacy_mirror which is being purged in sweeps 1-3 — coordinate)."
          },
          {
            "file": "tools/mcp/controllers/diagnostics_controller.c",
            "symbol": "projection-diff + diff_with_legacy MCP pass-throughs",
            "what": "DELETE the DEFINE_PT lines 128-134 (peers/mempool/znam/wallet/contacts/onionannouncements/hodlhistory projection_diff), h_zcl_diff_with_legacy (200-~260) + its k_routes row (667-673), and the k_routes rows lines 629-658 for the 7 projectiondiff tools. Update the file header comment (lines 9-17) to drop the projection-diff / diff_with_legacy bullets. The zcl_state route + g_state_subsystems_csv derivation (579, 714) STAYS unchanged (auto-shrinks)."
          },
          {
            "file": "lib/test/src/test_mcp_controllers.c",
            "symbol": "EXPECTED tool-count macros + tool-name list assertions",
            "what": "DECREMENT EXPECTED_TOTAL (49-65) by the removed tools (5 chain: diff_with_legacy_shadow, diff_staged_header_admit, utxo_projection_diff, block_index_diff, replay_verify; + ops: cutovermode, cutoverpreflight, diff_with_legacy, and the 7 *_projection_diff) and update the comment bullets (lines 53-65, 80-91). DECREMENT EXPECTED_OPS (66-85) and EXPECTED_CHAIN (86-92) accordingly. DELETE the tool-name list entries for the removed tools (e.g. lines 339 'zcl_cutovermode','zcl_cutoverpreflight'; 1143 'zcl_cutovermode'). DELETE mock_cutovermode_rpc (448), test_zcl_cutovermode_shape_and_dispatch (467-...), test_zcl_cutoverpreflight_shape_and_dispatch (528-...) and their calls at lines 1673-1674."
          },
          {
            "file": "lib/test/src/test.c",
            "symbol": "deleted-test registrations + ZCL_TEST_ONLY subsets",
            "what": "DELETE the registration lines: test_parity_diff_gate (661) + its ZCL_TEST_ONLY block (99-102), test_reorg_parity (736), test_reorg_projection_parity (737), test_conservation_diff_job (816), test_block_log_legacy (847), test_block_log_file (846 — only if block_log_file deleted), test_replay_verify (848), test_mutator (849), test_shadow_feeder (850), test_shadow_feeder_global (851), test_shadow_conservation (852), test_diff_with_legacy_shadow (853), test_shadow_replay_proof (854) + its ZCL_TEST_ONLY block (239-242), test_shadow_replay_full_driver (855), test_cutover_tip_parity (856), test_cutover_flip_dryrun (857), test_cutover_postflip_reorg (858), test_cutover_autorevert (859), test_cutover_preflight (860). KEEP test_projection_replay_invariant (811), test_stage_reorg_unwind_parity (738), test_utxo_projection (809), test_coins_view_projection, and all per-projection tests — they prove the single engine."
          },
          {
            "file": "Makefile",
            "symbol": "shadow_replay_proof tool build + MUTATOR_SRCS",
            "what": "DELETE line 169 `$(eval $(call BUILD_NODE_TOOL,shadow_replay_proof,tools/shadow_replay_proof.c))`. Once mutator/ is deleted, MUTATOR_SRCS (line 51 `MUTATOR_SRCS = $(wildcard mutator/src/*.c)`) resolves empty; remove the var and any reference to it in link objects."
          }
        ],
        "rewire": [
          {
            "file": "config/src/boot_services.c",
            "from": "boot_start_phase4_storage_shadow(ctx->datadir) — invoked as a SHADOW path that opens projections while legacy coins.db remains authoritative (line 2212), gated comments say 'observational only until cutover'",
            "to": "Run the same call as the UNCONDITIONAL storage engine: rename to boot_start_storage_engine(), drop the shadow framing, and ensure it returns FATAL (not warn) on event_log_open failure since the projections are now the only state. No -shadow flag, no seed-from-legacy."
          },
          {
            "file": "app/supervisors/src/staged_sync_supervisor.c",
            "from": "staged_sync_supervisor_register(ms, datadir) registers the 8 reducer stages PLUS the conservation_diff Job (line 482) under the chain supervisor domain",
            "to": "Register only the 8 reducer stages (header_admit -> ... -> tip_finalize); they ARE the sole writer of chain state. Drop the datadir-only-for-conservation_diff parameter. This is the single engine — no comparison child."
          },
          {
            "file": "app/jobs/src/header_admit_stage.c / validate_headers_stage.c / tip_finalize_stage.c",
            "from": "each stage branches on header_admit_get_mode()/validate_headers mode/tip_finalize mode (SHADOW vs AUTHORITATIVE) from cutover_modes.c to decide whether to be observational or authoritative",
            "to": "Always take the AUTHORITATIVE path: delete the mode lookup and the SHADOW branch; the stage's write to its log/projection is now the only writer. Remove the cutover_modes.h include."
          },
          {
            "file": "lib/validation/src/accept_block_header.c",
            "from": "header acceptance consults header_admit_get_mode()==AUTHORITATIVE and runs validate_headers_authoritative_guard (lines 193-362) to compare staged header_admit against legacy chain_active before trusting it",
            "to": "Remove the HEADER_ADMIT_MODE enum/decl (149-157), header_admit_get_mode usage, and the authoritative_guard divergence check; header acceptance flows through the single staged path unconditionally. (Consensus-sweep coordination, but the gate is the cutover apparatus.)"
          },
          {
            "file": "app/controllers/src/diagnostics_controller.c (RPC) + tools/mcp/controllers/*",
            "from": "AI/operator reaches chain-state truth via the diff/cutover tools (zcl_cutovermode, zcl_cutoverpreflight, zcl_diff_with_legacy, *_projection_diff, zcl_diff_with_legacy_shadow, zcl_diff_staged_header_admit, zcl_utxo_projection_diff, zcl_block_index_diff, zcl_replay_verify)",
            "to": "Single-engine introspection only: zcl_state subsystem=<projection|supervisor|boot|block_index> for projection internals, zcl_utxocommitment / zcl_dataintegrity for state hashes, zcl_syncstate / zcl_status for tip+health (network_tip - log_head). No engine-vs-engine comparison endpoint exists."
          }
        ],
        "notes": "SWEEP 4 read directly: config/src/boot_services.c, main.c (line 1659), config/include/config/boot.h, app/supervisors/src/staged_sync_supervisor.c, tools/mcp/controllers/{chain,ops,diagnostics}_controller.c, app/controllers/src/{diagnostics_controller,diagnostics_registry,projection_diff_controller,cutover_controller(_preflight)}.c, app/conditions/src/{condition_registry,cutover_canary_complete,cutover_no_forward_progress}.c, lib/validation/src/accept_block_header.c, lib/test/src/{test.c,test_mcp_controllers.c} + headers of every candidate test, and all five lint baselines.\n\nLINT BASELINE (SHRINK) FINDINGS — what the brief asked precisely:\n1) tools/scripts/file_size_ceiling_baseline.txt — DELETE all 4 data lines: legacy_import.c, sync_controller_catchup.c, sync_controller_import.c (legacy importers, sweeps 1-3), legacy_mirror_sync_service.c (purged). After the purge these app/ files no longer exist so the gate's grandfather entries vanish; file becomes header-comments only.\n2) tools/scripts/one_result_type_baseline.txt — DELETE the lines `app/services/src/cutover_modes.c` and `app/services/src/legacy_mirror_sync_service.c` (both deleted). The chain_state_repository.c / block_source_policy_* / utxo_recovery_* lines are NOT sweep-4 (they survive unless sweeps 1-3 delete the files).\n3) tools/scripts/one_write_path_baseline.txt — the legacy coins.db WRITE surfaces (66 lines) are sweep-1/2 scope, but the cutover-specific lines that vanish in this purge are the utxo_projection.c LEGACY-branch surfaces: lib/storage/src/utxo_projection.c:93 utxo_projection_set_author + lib/storage/include/storage/utxo_projection.h:50 decl (the author flag goes away when STAGE is the only writer). Coordinate with sweep 2.\n4) The brief's 'no_raw_sqlite_in_controllers_baseline.txt' does NOT exist; the actual gate is tools/scripts/raw_sqlite_allowlist.txt and it is ALREADY EMPTY (migration-complete, header comments only) — NOTHING to shrink there from this purge.\n\nKEY DISTINCTIONS I made by reading the code, not the names:\n- boot_start_phase4_storage_shadow STAYS (it opens event_log + the 8 projections = the engine) — only its shadow framing and the seed-from-legacy step are stripped, and it already runs unconditionally.\n- The 8 reducer-stage supervisor registrations STAY; only the 9th child (conservation_diff) is stripped.\n- Projection dumper rows (utxo/mempool/peers/znam) STAY in g_dumpers[]; only conservation_diff + cutover rows are stripped. The zcl_state enum_csv is DERIVED dynamically (diagnostics_controller.c:714) so it auto-shrinks — no manual enum edit needed (contradicts CLAUDE.md's static p_state[].enum_csv guidance, which no longer applies here).\n- zcl_replay_verify + replay_verify_service.c: classified DELETE because it ONLY reads the legacy on-disk block log (block_log_legacy), which is being purged; it is the cutover 'PROVE Tier-1' tool, not a single-engine diagnostic.\n- block_log_file.c / block_log_legacy.c: after deleting conservation_diff_job + replay_verify_service + shadow_replay_proof, grep shows ZERO remaining non-test .c consumers, so both adapters are delete-eligible — but I flagged 'VERIFY no staged body source consumes block_log_file' before pulling it, since the body_fetch/body_persist sources are out of sweep-4 scope.\n- test_reorg_parity.c / test_reorg_projection_parity.c flagged DELETE (cutover keystone / legacy-disconnect-emit proofs) but I marked test_reorg_parity 'VERIFY' since it may double as the general single-engine reorg proof; test_stage_reorg_unwind_parity.c (the STAGE-side reorg proof) explicitly STAYS.\n- test_projection_replay_invariant.c, test_utxo_projection.c, test_coins_view_projection.c, and per-projection tests STAY — they prove the surviving engine."
      },
      {
        "area": "SWEEP 5 — single-engine (KEEP path) completeness + gaps after the duality purge",
        "delete_files": [],
        "strip_symbols": [
          {
            "file": "lib/storage/src/utxo_projection.c",
            "symbol": "g_author + utxo_projection_set_author/get_author (lines 85-102) and the UTXO_AUTHOR_STAGE guard `if (... == UTXO_AUTHOR_STAGE)` at utxo_apply_stage.c:260, utxo_apply_delta.c:730",
            "what": "After the purge the stage is the SOLE author UNCONDITIONALLY. Strip the author flag + both LEGACY branches in update_coins.c (lines 53, 77 — the whole shadow-emit functions go since update_coins.c is on the legacy write path being deleted) and make emit_delta() in utxo_apply_stage.c run with no author gate. GAP: the gate currently defaults to LEGACY so emit_delta is a no-op in production today; removing the gate is what makes the stage actually populate the projection. KEEP: emit_delta itself + utxo_projection_emit_add/emit_spend."
          },
          {
            "file": "app/jobs/src/tip_finalize_stage.c",
            "symbol": "is_authoritative() (line 485) + tip_finalize_get/set_mode (590-604) routing through cutover_modes; preconditions_ok (258) and reorg_depth_from (241) which read ms->chain_active/block_index",
            "what": "tip_finalize runs in SHADOW (LOG_INFO at line 550 literally says \"shadow mode\"). After purge it must be AUTHORITATIVE UNCONDITIONALLY — strip the cutover_modes mode plumbing and make is_authoritative() return true always (or delete the gate and always register the active_chain authority). GAP/NOTES: tip_finalize writes only the SQLite tip_finalize_log + progress.kv cursor; it does NOT itself flip active_chain. Its authority bridge (active_chain_register_authority at line 527) only takes effect when is_authoritative() returns true — that flip is the load-bearing cutover. The tip becomes readable via chainstate.c active_chain_tip/height authority shim (see rewire below)."
          },
          {
            "file": "config/src/boot_services.c",
            "symbol": "boot_start_phase4_storage_shadow (line 1104) — specifically the utxo_projection_seed_from_legacy block (lines 1196-1218) and the \"shadow\" framing of the whole function",
            "what": "This is the ONLY production wiring that opens the event log + projections (event_log_open line 1129, event_log_set_singleton line 1135, *_projection_open + catch_up). It MUST SURVIVE and be promoted from \"shadow\" to the real boot path. STRIP: the one-time seed-from-legacy coins.db (1196-1218) which depends on the legacy coins.db being deleted — replace with the cold-start gap below. KEEP+REWIRE: every event_log_open / set_singleton / *_projection_open / *_projection_catch_up call. GAP: rename/relocate out of a \"phase4 shadow\" helper into the mainline boot sequence so it is not skippable."
          },
          {
            "file": "lib/validation/src/coins_view_stage_backing.c (lib/storage/src/coins_view_stage_backing.c)",
            "symbol": "coins_view_select_connect_backing (line 79): the `if (utxo_projection_get_author() != UTXO_AUTHOR_STAGE) { *out = *legacy; }` branch (line 91) and the NULL-proj fallback-to-legacy (lines 102-117)",
            "what": "After purge there is no legacy view to fall back to. Strip the author gate (always select projection-backed reads) and convert the silent legacy fallback into a hard LOG_FAIL when proj/sb is NULL — a missing projection must be fatal, not a quiet downgrade to a deleted engine. NOTES: this file STAYS as the projection-backed read adapter for connect_tip; only its legacy-branch and fallback are stripped."
          }
        ],
        "rewire": [
          {
            "file": "config/src/boot.c",
            "from": "boot block-index + coins.db load path (load_block_index_sqlite/flat line 1804-1806, load_block_index line 1874, coins_view_db_open backing coins_tip) PLUS the legacy importer (controllers/legacy_import.h include line 79, cold_import_pending_anchor resolution lines 799-900)",
            "to": "GAP — NEW CODE REQUIRED for cold-start. Today the chain block_index map (g_state.map_block_index) and the BLOCK_HAVE_DATA/BLOCK_VALID nStatus bits that EVERY stage reads (active_chain_at + preconditions_ok in tip_finalize_stage.c:258, utxo_apply_stage.c:225) are populated ONLY by the legacy importer + legacy accept_block.c/process_block_core.c. After deleting the importer + coins.db, boot must: (1) open the event_log unconditionally, (2) replay block_index_projection to rebuild the block_index map (block_index_projection_get_by_height exists at block_index_projection.c:548 but has ZERO production readers — wire it as the index source), (3) replay utxo_projection.catch_up to rebuild the UTXO set, (4) seed tip_finalize cursor from progress.kv. Write a boot_rebuild_from_log() that drives steps 2-4 in place of load_block_index_sqlite+legacy import."
          },
          {
            "file": "app/jobs/src/header_admit_stage.c (+ validate_headers_stage.c, body_fetch_stage.c, body_persist_stage.c)",
            "from": "stages READ active_chain_at / map_block_index / nStatus but the legacy connect path (accept_block.c, process_block_core.c) is what WRITES the block_index map + sets BLOCK_HAVE_DATA",
            "to": "GAP — NEW CODE REQUIRED. With the legacy connect path gone, the reducer must itself create/insert block_index entries (header_admit) and set BLOCK_HAVE_DATA after body_persist writes the block to disk_block_io. Today header_admit only writes the SQLite header_admit_log + emits to block_index_projection; it does NOT call chainstate_insert_block_index or set nStatus (confirmed: no block_map_insert/nStatus writes in header_admit_stage.c or validate_headers_stage.c). The single engine needs the stage to own the in-memory block_index map mutation, OR all stages must be rewired to read block_index_projection instead of ms->map_block_index. Pick one: it is the central cold-start/advance gap."
          },
          {
            "file": "app/controllers/src/blockchain_controller_blocks.c",
            "from": "getblock reads block_map_find(&ctx->main_state->map_block_index, &hash) (lines 206, 238) + disk_block_io (legacy block index map + flat files)",
            "to": "NOTES (read serving): block bodies come from disk_block_io which the single engine still writes via body_persist_stage — that read STAYS. But the block_index lookup must resolve from whatever populates map_block_index in the single engine (see boot.c rewire). If the map is rebuilt from block_index_projection at boot, getblock works unchanged; if not, rewire block_map_find -> block_index_projection_get."
          },
          {
            "file": "app/controllers/src/blockchain_controller_chain.c",
            "from": "gettxoutsetinfo + balance/listunspent read coins_tip (coins_view_sqlite over coins.db) — gettxoutsetinfo at line 335 fails if \"coins database not available\"; coins_view_cache_flush callers across the baseline",
            "to": "GAP — REWIRE reads to the projection. The transparent-UTXO reads (gettxoutsetinfo, the wallet's UTXO scan behind balance/listunspent) currently hit coins.db via coins_tip. After coins.db is deleted these must read utxo_projection (utxo_projection_get_coins at utxo_projection.c:543 + utxo_projection_count at 603 already exist and are byte-compatible). Rewire rpc_blockchain_set_coins_db / the coins_tip consumers to a projection-backed coins_view (coins_view_projection.c exists). This is the read-serving completeness gap."
          },
          {
            "file": "lib/validation/src/connect_tip.c / activate_best_chain.c / disconnect_tip.c",
            "from": "the legacy chain-advance via process_new_block -> connect_tip -> coins_view_cache_flush -> coins_view_sqlite_batch_write (the 64 surfaces in one_write_path_baseline.txt)",
            "to": "NOTES — these ARE the legacy write engine being deleted in SWEEP 1-4; SWEEP 5's concern is that once they are gone, the reducer (utxo_apply_stage + tip_finalize_stage) is the ONLY thing advancing the tip. The reducer is COMPLETE for forward-apply (utxo_apply_compute_block_delta + emit_delta + utxo_apply_delta_persist for reorg-unwind) and tip publication (active_chain authority shim). The single missing piece is index/status population (the two GAP rewires above). No new code needed inside the stages for the apply math itself."
          }
        ],
        "notes": "VERDICT: the KEEP engine's DURABLE+APPLY core is complete; its INDEX-POPULATION and READ-SERVING wiring are NOT, and depend on the legacy engine being deleted. Anchored facts:\\n\\n1) DURABLE LOG — complete. lib/storage/src/event_log.c is a self-contained append-only log with crash-recovery (recover_truncate_partial line 254 truncates partial tails on open), CRC32C-verified streaming (event_log_stream line 554), and a stable on-disk format. It IS opened in production (boot_services.c:1129 event_log_open + 1135 event_log_set_singleton) — NOTE: this is the storage log; the similarly-named event_log_init() at boot.c:537 is the UNRELATED in-memory observability ring in lib/event/src/event.c:266 — do not confuse them.\\n\\n2) PROJECTIONS — complete + driven, but in SHADOW. All 8 projections (utxo/block_index/mempool/peers/wallet/znam/contacts/onion/hodl) are opened and catch_up-driven at boot_services.c boot_start_phase4_storage_shadow (utxo lines 1178-1194, wallet 1278-1284, block_index 1229-1237, etc). utxo_projection.c has full read primitives: get_coins (543), count (603), commitment (627, byte-identical SHA3 to legacy). catch_up (365) is a single IMMEDIATE-txn idempotent replay — survives restart cleanly.\\n\\n3) APPLY/REDUCE — complete for forward + reorg. utxo_apply_stage (compute_block_delta + emit_delta line 172 + utxo_apply_delta_persist for inverse-delta/reorg-unwind) and tip_finalize_stage (work-delta + utxo-count-conservation check + cursor advance) are functionally complete. tip = tip_finalize's progress.kv cursor; its active_chain authority shim (chainstate.c active_chain_tip line 251 / height line 341 / set_tip line 328) republishes the stage tip for ALL readers WHEN is_authoritative() is true.\\n\\n4) THE TWO REAL GAPS (need NEW code, not just deletion):\\n   (a) BLOCK-INDEX + nStatus POPULATION. Every stage READS ms->map_block_index via active_chain_at and gates on BLOCK_HAVE_DATA / BLOCK_VALID_SCRIPTS (tip_finalize preconditions_ok line 258; utxo_apply line 225). Those bits are written ONLY by legacy accept_block.c/process_block_core.c — confirmed header_admit_stage.c + validate_headers_stage.c contain NO block_map_insert/nStatus/chainstate_insert writes. After the legacy connect path is deleted, NOTHING populates the index the stages read. Fix: either (i) make header_admit own block_index insertion + body_persist set BLOCK_HAVE_DATA, or (ii) rebuild map_block_index from block_index_projection at boot and rewire stage reads. This is the cold-start AND advance blocker.\\n   (b) READ-SERVING off coins.db. getblock uses block_map_find on the legacy map (blockchain_controller_blocks.c:206,238); gettxoutsetinfo/balance/listunspent read coins_tip = coins_view_sqlite over coins.db (blockchain_controller_chain.c:335). After coins.db is deleted these must be rewired to utxo_projection (get_coins/count) and a projection-backed block index. block_index_projection_get_by_height (block_index_projection.c:548) EXISTS but has ZERO production readers — it is the intended seam, currently unwired.\\n\\n5) AUTHORITY FLIP is the cutover lever, not new logic. Three gates today force SHADOW and default to legacy: utxo_projection g_author=LEGACY (utxo_projection.c:91), tip_finalize mode via cutover_modes (is_authoritative line 485), coins_view_stage_backing author!=STAGE legacy fallback (line 91). All three must be stripped to make the single engine run UNCONDITIONALLY (no flag, no shadow) — this is consistent with the owner mandate. Once stripped: emit_delta authors the log, tip_finalize publishes the tip, projection-backed reads serve. But stripping these WITHOUT first closing gap 4(a) yields a node whose stages have no block_index to consume — a non-building/non-advancing intermediate is acceptable per the mandate, but the END STATE requires 4(a)+4(b) new code to be coherent.\\n\\n6) The seed-from-legacy step (utxo_projection_seed_from_legacy, called boot_services.c:1207) is the bridge that copies coins.db into the projection so SHA3 matches — it MUST be removed (it reads the deleted coins.db) and replaced by genesis-up replay from the event log at cold-start (gap 4a/boot.c rewire)."
      }
    ],
    "manifest": {
      "stages": [
        {
          "n": 1,
          "title": "Delete leaf apparatus TESTS + their registry rows",
          "does": "Delete the 19 apparatus test .c files (test_shadow_feeder, test_shadow_feeder_global, test_shadow_conservation, test_diff_with_legacy_shadow, test_shadow_replay_proof, test_shadow_replay_full_driver, test_reorg_parity, test_reorg_projection_parity, test_conservation_diff_job, test_mutator, test_cutover_preflight, test_cutover_flip_dryrun, test_cutover_autorevert, test_cutover_postflip_reorg, test_cutover_tip_parity, test_parity_diff_gate, test_block_log_legacy, test_block_log_file, test_replay_verify). Strip their registrations: test_parallel.c X(...) macro rows (lines 86-87,120,136-143,155), test.c sequential calls (lines 101,660,735-736,844-849), and test_helpers.h extern decls (263,329,330,425,426,450-453). VERIFY test_reorg_parity is not the sole single-engine reorg proof before deleting (test_stage_reorg_unwind_parity is the STAGE-side proof and STAYS). This stage stands alone — removing tests dangles nothing because their target .c files still exist until stage 2.",
          "depends_on": "none"
        },
        {
          "n": 2,
          "title": "Rewire the -shadow ingest hook out of the hot net path",
          "does": "In lib/net/src/msg_blocks.c delete the `extern void shadow_feeder_global_observe(...)` decl (267-269) and the call at 411. Net ingest no longer mirrors accepted blocks into a shadow log. This must precede deleting shadow_feeder_global.c so its only hot-path caller is gone first.",
          "depends_on": "none"
        },
        {
          "n": 3,
          "title": "Strip the -shadow boot wiring + conservation_diff Job wiring + cutover conditions registration",
          "does": "config/src/boot_services.c: delete includes (shadow_feeder.h 159, shadow_feeder_global.h 160, mutator/mutator.h 161, conservation_diff_job.h 25), the g_shadow_mutator/g_shadow_feeder statics (162-163), boot_shadow_feeder_start/_disarm/_dispose (165-237), and their call sites (3016,3241,3254), plus conservation_diff_job_shutdown() (3167). app/supervisors/src/staged_sync_supervisor.c: delete conservation_diff include (25), CONSERVATION_DIFF_BATCH_PER_TICK, cd_tick/cd_stall/staged_conservation_diff_register (414-462) and the call at 482; keep the 8 reducer-stage registrations. app/conditions/src/condition_registry.c: delete register_cutover_no_forward_progress/canary_complete decls (23-24) and calls (49-50). main.c: delete -shadow parse (1659). config/include/config/boot.h: delete shadow_feeder_enabled field (54-58). This removes every remaining caller of the shadow feeder / mutator / conservation_diff / cutover conditions before those files are deleted.",
          "depends_on": "stage 2"
        },
        {
          "n": 4,
          "title": "Delete MCP/RPC apparatus surfaces (chain/ops/diagnostics controllers + diagnostics_internal decls + RPC table + metrics gauge)",
          "does": "tools/mcp/controllers/chain_controller.c: delete includes (12-16,23,25), handlers diff_status_name(83), h_zcl_diff_with_legacy_shadow(95), h_zcl_diff_staged_header_admit(296), h_zcl_replay_verify(390), h_zcl_block_index_diff(520), h_zcl_utxo_projection_diff(720), param specs, and k_routes rows (1027-1068). tools/mcp/controllers/ops_controller.c: delete h_zcl_cutovermode(295)/h_zcl_cutoverpreflight(324), p_cutovermode(841)/p_cutoverpreflight(849), k_routes rows (946-960). tools/mcp/controllers/diagnostics_controller.c: delete DEFINE_PT rows (128-134), h_zcl_diff_with_legacy(200) + its route (667-673), the 7 projectiondiff routes (629-658), header comment (9-17). app/controllers/src/diagnostics_controller.c: delete RPC table rows cutovermode(32)/cutoverpreflight(33)/getmirrorstatus(37)/7 projectiondiff(38-47) + header comment (12,16). app/controllers/include/controllers/diagnostics_internal.h: delete diag_rpc_cutovermode/cutoverpreflight/getmirrorstatus/7 projectiondiff decls + cutover_push_canary_state (43-85). app/controllers/src/diagnostics_registry.c: delete conservation_diff include (51) + dumper (447-449), cutover include (43) + cutover dumper (458-463), and edit utxo_projection dumper text (480-485). tools/mcp/metrics.c: delete g_shadow_divergence_count (109), mcp_metrics_set_shadow_divergence (299), Prometheus block (809-815). tools/mcp/metrics.h: delete decl (231). lib/test/src/test_mcp_controllers.c: decrement EXPECTED_TOTAL/OPS/CHAIN, delete cutover tool-name list entries + mock_cutovermode_rpc + the two cutover shape tests and their calls. zcl_state enum_csv is derived dynamically (diagnostics_controller.c:714) so it auto-shrinks. This severs all external handlers before the controller/use-case .c files are deleted.",
          "depends_on": "stage 1"
        },
        {
          "n": 5,
          "title": "Delete the cutover controllers, conditions, services, diff/replay use-cases, mutator tree, block_log legacy adapters, and the apparatus jobs",
          "does": "Now that all callers are gone (stages 2-4), delete the apparatus files: cutover_controller.c/_preflight.c, projection_diff_controller.c, cutover_canary_complete.c, cutover_no_forward_progress.c, cutover_modes.c(+.h), conservation_diff_job.c(+.h), replay_verify_service.c(+.h), diff_with_legacy_shadow.c(+.h), shadow_replay_proof.c(+.h), tools/shadow_replay_proof.c, shadow_feeder.c/_global.c/conservation.c(+.h), mutator/ tree (3 src + 3 hdr), block_log_legacy.c/_rpc.c/block_log_file.c(+.h). KEEP ports/include/ports/block_log_port.h and utxo_snapshot_port.h — msg_blocks.c and validate_block.c use the port structs, only the legacy adapters go. Strip the cutover_controller set_author line as part of its deletion (it is the sole production set_author caller). STRIP the three projection diff_legacy fns (contacts_projection.c:325, onion_announcements_projection.c:281, hodl_history_projection.c:273) — projections stay, only their compare-against-legacy fns die. Makefile: delete BUILD_NODE_TOOL shadow_replay_proof (169); MUTATOR_SRCS (51) auto-empties when mutator/ is gone.",
          "depends_on": "stage 2,stage 3,stage 4"
        },
        {
          "n": 6,
          "title": "Collapse the SHADOW/AUTHORITATIVE stage duality — make the reducer the unconditional writer",
          "does": "Strip the cutover mode plumbing from the 3 stages (their cutover_modes.h dependency is dead after stage 5). app/jobs/src/header_admit_stage.c: inline authoritative_admit unconditionally (drop the get_mode==AUTHORITATIVE guard at 279), delete set/get_mode + authoritative_hook + header_admit_stage_diff. app/jobs/src/validate_headers_stage.c: always authoritative-mark, drop set/get_mode. app/jobs/src/tip_finalize_stage.c: make is_authoritative() return true always (485), drop set/get_mode, update shadow-mode log (550). Delete the corresponding header decls: header_admit_stage.h (mode enum 66-68, set/get 72-73, hook 107-113, diff 185), validate_headers_stage.h (enum 99-101, set/get 103-104), tip_finalize_stage.h (enum 27-29, set/get 34-35). lib/validation/src/accept_block_header.c: delete the HEADER_ADMIT_MODE enum (153-157), header_admit_get_mode usage (334), and validate_headers_authoritative_guard (193-362, called 322,404) — header acceptance flows through the single staged path. This makes the staged pipeline run authoritatively with no mode flag.",
          "depends_on": "stage 5"
        },
        {
          "n": 7,
          "title": "Strip the UTXO author flag — projection reads + stage emit become unconditional",
          "does": "lib/storage/src/utxo_projection.c: delete g_author (91) + set_author/get_author (93-102). lib/storage/include/storage/utxo_projection.h: delete utxo_author_t enum (45-48), set/get_author decls (50-51), B3 doc (34-44). lib/storage/src/coins_view_stage_backing.c: strip the author-gated LEGACY early-return (91-94) so the projection composite is selected unconditionally, and convert the NULL-proj fallback (102-117) to a hard LOG_FAIL (missing projection is fatal, not a silent downgrade). app/jobs/src/utxo_apply_stage.c: strip the get_author==STAGE gate at emit_delta (260) so it runs on every verified block; update stale comments (256-259) + shadow-mode log (358). app/jobs/src/utxo_apply_delta.c: strip the get_author==STAGE reorg-unwind gate (730). After this the projection is the unconditional reader for connect_block AND utxo_apply_stage is the unconditional, sole UTXO event author. The cutover_controller.c set_author flip already died in stage 5.",
          "depends_on": "stage 5,stage 6"
        },
        {
          "n": 8,
          "title": "GAP: make the reducer own block_index + nStatus population (cold-start + advance)",
          "does": "NEW CODE. Today the in-memory block_index map (ms->map_block_index) and the BLOCK_HAVE_DATA/BLOCK_VALID_SCRIPTS nStatus bits that every stage reads (tip_finalize preconditions_ok:258, utxo_apply:225) are written ONLY by the legacy accept_block.c/process_block_core.c connect path. Before the legacy coins.db write path is deleted (stage 9), the reducer must itself populate the index: either header_admit_stage.c inserts block_index entries + body_persist_stage sets BLOCK_HAVE_DATA, OR all stages are rewired to read block_index_projection (block_index_projection_get_by_height exists at block_index_projection.c:548 with zero production readers — wire it). Add boot_rebuild_from_log() in config/src/boot.c that drives: open event_log unconditionally -> replay block_index_projection to rebuild the map -> replay utxo_projection.catch_up -> seed tip_finalize cursor from progress.kv, in place of load_block_index_sqlite + the legacy importer. This is the central cold-start/advance blocker; must land before stage 9.",
          "depends_on": "stage 6,stage 7"
        },
        {
          "n": 9,
          "title": "GAP+STRIP: retire the legacy coins.db DURABLE write path and repoint consensus reads to the projection",
          "does": "NEW CODE + STRIP, gated on stage 8 (projection seeded + caught-up). Repoint the two consensus-critical reads to the projection FIRST: connect_tip.c SHA3 checkpoint (554) utxo_commitment_sha3_compute(coins.db) -> utxo_projection_commitment; coins_view_sqlite.c boot tip-consistency check (280) -> projection. Then strip durable coins.db writes: lib/validation/src/update_coins.c delete update_coins_emit_utxo_*_shadow (44-88) + g_utxo_event counters (30-42) + call sites (145,187); connect_block.c delete the disconnect-side shadow-emit calls (752,799); connect_tip.c remove flush_coins_if_needed durable flushes (548,718,968) keeping coins_view_cache_flush(&view) only as a RAM accelerator; disconnect_tip.c remove coins.db durable flush (192) + node_db_sync_disconnect_block (208); process_block_flush_policy.c strip the coins.db batch-write branch (235-252) keeping only Sapling-tree checkpoint. chain_state_repository.c strip coins_best_block persistence (csr_persist_coins_best_locked 151-203, 686-695) + coins_tip set_best_block (631,658,710) — tip_finalize progress.kv cursor is definitional; keep active_chain_set_tip + validation guards. chain_tip.c strip chain_tip_fsync_barrier coins.db cacheflush (57-84,178); keep active_chain_set_tip. Then DELETE coins_view_sqlite.c (the legacy UTXO authority — no production writer remains). SHRINK one_write_path_baseline lines for every deleted call.",
          "depends_on": "stage 8"
        },
        {
          "n": 10,
          "title": "GAP+REWIRE: read-serving off coins.db -> projection",
          "does": "NEW CODE. blockchain_controller_chain.c: gettxoutsetinfo (335) + balance/listunspent read coins_tip (coins_view_sqlite over coins.db) — rewire to utxo_projection (utxo_projection_get_coins:543, utxo_projection_count:603 already exist, byte-compatible) via a projection-backed coins_view (coins_view_projection.c exists). blockchain_controller_blocks.c: getblock block_map_find (206,238) resolves from whatever populates map_block_index — works unchanged if stage 8 rebuilt the map from block_index_projection, else rewire to block_index_projection_get. Block bodies still come from disk_block_io (written by body_persist_stage) — that read STAYS. Closes the read-serving completeness gap once coins.db is gone.",
          "depends_on": "stage 9"
        },
        {
          "n": 11,
          "title": "Delete the legacy importers + always-on auto-import; rewire cold-start to fast_sync/projection",
          "does": "DELETE sync_controller_import.c (node_db_sync_import_utxos pipeline, the LevelDB->legacy utxos authority loader). REWIRE its 5 callers first: main.c delete --importchainstate subcommand (1245-1300, call 1274); utxo_recovery_restore.c strip utxo_recovery_import_ldb body (52, calls 126/129) keeping utxo_recovery_restore_chain_tip (372); sync_controller_catchup.c strip the import-job half (node_db_sync_import_job_* 968-1016) keeping node_db_sync_catchup/wallet_keys/mempool; blockchain_controller_admin.c delete/retarget importutxos RPC (285). config/src/boot.c strip the 3 no_legacy_auto_import blocks (1892 LevelDB block-index import, 2117 hard-link ~/.zclassic/blocks, 2292-2309 utxo_recovery_import_ldb) — superseded by boot_rebuild_from_log() (stage 8); main.c remove -nolegacyimport flag (1660) + ctx.no_legacy_auto_import + help text (718). legacy_import.c: delete (wallet-from-foreign-blockfiles), rewire snapshot_controller_import.c:477 + wallet_diagnostic_repair.c:205 to wallet_projection fold + on-chain rescan (replaywalletfromchain/rescanblockchain); drop the dead legacy_import.h includes in the wallet_diagnostic_* files + boot.c:79. Drop the legacy_attach_tip_* progress keys + readers (accept_block_header.c:181, validate_headers_validator.c:269) and the g_body_pull relax-flag (process_block.h:70-96, connect_tip.c:694,777) once cold-start no longer body-pulls. legacy_mirror_sync_service.c is KEEP (post-B7 monitor-only, cfg.enabled defaults false) — not an importer; stop treating it as authoritative sync but do not delete.",
          "depends_on": "stage 8,stage 9,stage 10"
        },
        {
          "n": 12,
          "title": "SHRINK lint baselines + final coherence pass",
          "does": "tools/scripts/one_result_type_baseline.txt: delete cutover_modes.c (23). (legacy_mirror_sync_service.c line 24 STAYS — file is KEEP per stage 11.) tools/scripts/file_size_ceiling_baseline.txt: delete legacy_import.c (13), sync_controller_import.c (15) once those files are deleted; sync_controller_catchup.c (14) shrinks but file stays so keep its line if still over ceiling; legacy_mirror_sync_service.c (16) STAYS. tools/scripts/one_write_path_baseline.txt: delete utxo_projection.h set_author decl (37) + utxo_projection.c set_author def (42), coins_view_sqlite_batch_write* lines (35-41 once coins_view_sqlite.c deleted), connect_tip/disconnect_tip/flush_policy coins.db flush lines (59,61,64,65), and the coins_view_cache_flush admin/repair/boot helper lines (3-29) that lose meaning; KEEP active_chain_set_tip + process_new_block lines. raw_sqlite_allowlist.txt is already empty — nothing to shrink. Final: rename boot_start_phase4_storage_shadow -> boot_start_storage_engine (drop shadow framing, make event_log_open FATAL, remove utxo_projection_seed_from_legacy block 1196-1218 which read the deleted coins.db); make boot run it unconditionally as mainline. Run make lint + make test_parallel; confirm no dangling refs.",
          "depends_on": "stage 11"
        }
      ],
      "delete_files": [
        "adapters/inbound/src/shadow_feeder.c",
        "adapters/inbound/include/adapters/inbound/shadow_feeder.h",
        "adapters/inbound/src/shadow_feeder_global.c",
        "adapters/inbound/include/adapters/inbound/shadow_feeder_global.h",
        "adapters/inbound/src/shadow_conservation.c",
        "adapters/inbound/include/adapters/inbound/shadow_conservation.h",
        "application/operations/src/diff_with_legacy_shadow.c",
        "application/operations/include/application/operations/diff_with_legacy_shadow.h",
        "application/operations/src/shadow_replay_proof.c",
        "application/operations/include/application/operations/shadow_replay_proof.h",
        "tools/shadow_replay_proof.c",
        "app/jobs/src/conservation_diff_job.c",
        "app/jobs/include/jobs/conservation_diff_job.h",
        "app/services/src/cutover_modes.c",
        "app/services/include/services/cutover_modes.h",
        "app/controllers/src/cutover_controller.c",
        "app/controllers/src/cutover_controller_preflight.c",
        "app/controllers/src/projection_diff_controller.c",
        "app/conditions/src/cutover_no_forward_progress.c",
        "app/conditions/src/cutover_canary_complete.c",
        "app/services/src/replay_verify_service.c",
        "app/services/include/services/replay_verify_service.h",
        "adapters/outbound/persistence/src/block_log_legacy.c",
        "adapters/outbound/persistence/include/adapters/outbound/persistence/block_log_legacy.h",
        "adapters/outbound/persistence/src/block_log_legacy_rpc.c",
        "adapters/outbound/persistence/include/adapters/outbound/persistence/block_log_legacy_rpc.h",
        "adapters/outbound/persistence/src/block_log_file.c",
        "adapters/outbound/persistence/include/adapters/outbound/persistence/block_log_file.h",
        "mutator/src/mutator.c",
        "mutator/src/cmd.c",
        "mutator/src/input_queue.c",
        "mutator/include/mutator/mutator.h",
        "mutator/include/mutator/cmd.h",
        "mutator/include/mutator/input_queue.h",
        "lib/storage/src/coins_view_sqlite.c",
        "app/controllers/src/sync_controller_import.c",
        "app/controllers/src/legacy_import.c",
        "lib/test/src/test_shadow_feeder.c",
        "lib/test/src/test_shadow_feeder_global.c",
        "lib/test/src/test_shadow_conservation.c",
        "lib/test/src/test_diff_with_legacy_shadow.c",
        "lib/test/src/test_shadow_replay_proof.c",
        "lib/test/src/test_shadow_replay_full_driver.c",
        "lib/test/src/test_reorg_parity.c",
        "lib/test/src/test_reorg_projection_parity.c",
        "lib/test/src/test_conservation_diff_job.c",
        "lib/test/src/test_mutator.c",
        "lib/test/src/test_cutover_preflight.c",
        "lib/test/src/test_cutover_flip_dryrun.c",
        "lib/test/src/test_cutover_autorevert.c",
        "lib/test/src/test_cutover_postflip_reorg.c",
        "lib/test/src/test_cutover_tip_parity.c",
        "lib/test/src/test_parity_diff_gate.c",
        "lib/test/src/test_block_log_legacy.c",
        "lib/test/src/test_block_log_file.c",
        "lib/test/src/test_replay_verify.c"
      ],
      "strip_rewire": [
        {
          "file": "lib/net/src/msg_blocks.c",
          "change": "REWIRE: delete the `extern void shadow_feeder_global_observe(...)` decl (267-269) and the call at 411. Net ingest no longer mirrors accepted blocks into the shadow log. (block_log_port.h usage at 291 STAYS — it is the streaming buffer struct, not the legacy adapter.)"
        },
        {
          "file": "config/src/boot_services.c",
          "change": "STRIP: includes shadow_feeder.h(159)/shadow_feeder_global.h(160)/mutator.h(161)/conservation_diff_job.h(25); statics g_shadow_mutator/g_shadow_feeder(162-163); boot_shadow_feeder_start/_disarm/_dispose(165-237); call sites 3016/3241/3254; conservation_diff_job_shutdown()(3167). REWIRE: rename boot_start_phase4_storage_shadow->boot_start_storage_engine, drop shadow framing, remove utxo_projection_seed_from_legacy block(1196-1218), make event_log_open FATAL; keep all event_log_open/set_singleton/*_projection_open/catch_up calls (the engine that stays)."
        },
        {
          "file": "app/supervisors/src/staged_sync_supervisor.c",
          "change": "STRIP: conservation_diff include(25), CONSERVATION_DIFF_BATCH_PER_TICK, cd_tick/cd_stall/staged_conservation_diff_register(414-462), call at 482. KEEP the 8 reducer-stage registrations (header_admit..tip_finalize); optionally drop the now-conservation-only datadir param and rename SHADOW_STAGE_QUIET_US."
        },
        {
          "file": "app/conditions/src/condition_registry.c",
          "change": "STRIP: register_cutover_no_forward_progress decl(23) + call(49); register_cutover_canary_complete decl(24) + call(50)."
        },
        {
          "file": "main.c",
          "change": "STRIP: -shadow parse(1659); -nolegacyimport parse(1660) + ctx.no_legacy_auto_import + help(718); --importchainstate subcommand(1245-1300, node_db_sync_import_utxos call 1274); --importblockindex(1393) unless retargeted to seed event_log. Also drop legacy_attach_tip_* progress-key usage if any leaks here."
        },
        {
          "file": "config/include/config/boot.h",
          "change": "STRIP: struct app_context bool shadow_feeder_enabled field + comment (54-58); bool no_legacy_auto_import field (once boot auto-import blocks are gone)."
        },
        {
          "file": "app/controllers/src/diagnostics_controller.c",
          "change": "STRIP RPC table rows: cutovermode(32), cutoverpreflight(33), getmirrorstatus(37), peers/mempool/znam/wallet/contacts/onionannouncements/hodlhistory projectiondiff(38-47); update header comment(12,16). KEEP dumpstate/getnodelog/dbquery/probezclassicd. zcl_state enum_csv auto-shrinks (derived at 714)."
        },
        {
          "file": "app/controllers/include/controllers/diagnostics_internal.h",
          "change": "STRIP decls: diag_rpc_cutovermode(43), diag_rpc_cutoverpreflight(47), cutover_push_canary_state(54), diag_rpc_getmirrorstatus(70), the 7 diag_rpc_*projectiondiff(72-85), + the cutover-canary shared-state comment(50-52)."
        },
        {
          "file": "app/controllers/src/diagnostics_registry.c",
          "change": "STRIP: conservation_diff include(51) + dumper entry(447-449); cutover_modes.h include(43) + cutover dumper entry(458-463). EDIT utxo_projection dumper description(480-485) to drop 'Shadow-mode'/'diff via zcl_utxo_projection_diff before cutover'. KEEP projection dumper rows."
        },
        {
          "file": "tools/mcp/controllers/chain_controller.c",
          "change": "STRIP: includes(12-16,23,25); handlers diff_status_name(83), h_zcl_diff_with_legacy_shadow(95), h_zcl_diff_staged_header_admit(296), h_zcl_replay_verify(390), h_zcl_block_index_diff(520), h_zcl_utxo_projection_diff(720); param specs p_diff_shadow/p_diff_staged/p_replay_verify; k_routes rows(1027-1068). KEEP real chain tools (getblock, utxocommitment, mmb, syncstate)."
        },
        {
          "file": "tools/mcp/controllers/ops_controller.c",
          "change": "STRIP: h_zcl_cutovermode(295), h_zcl_cutoverpreflight(324), p_cutovermode(841), p_cutoverpreflight(849), k_routes rows(946-960), header tool-list ref. KEEP zcl_mirror_status only if it does not route through deleted projection_diff_controller."
        },
        {
          "file": "tools/mcp/controllers/diagnostics_controller.c",
          "change": "STRIP: DEFINE_PT projection_diff rows(128-134), h_zcl_diff_with_legacy(200) + its route(667-673), the 7 *projectiondiff k_routes rows(629-658), header comment(9-17). zcl_state route + g_state_subsystems_csv(714) STAY (auto-shrink)."
        },
        {
          "file": "tools/mcp/metrics.c",
          "change": "STRIP: g_shadow_divergence_count static(109), mcp_metrics_set_shadow_divergence(299-303), zcl_shadow_divergence_count Prometheus block(809-815)."
        },
        {
          "file": "tools/mcp/metrics.h",
          "change": "STRIP: mcp_metrics_set_shadow_divergence decl(231) + doc comment(220)."
        },
        {
          "file": "lib/test/src/test_mcp_controllers.c",
          "change": "STRIP: decrement EXPECTED_TOTAL/EXPECTED_OPS/EXPECTED_CHAIN by removed tools (5 chain: diff_with_legacy_shadow, diff_staged_header_admit, utxo_projection_diff, block_index_diff, replay_verify; ops: cutovermode, cutoverpreflight, diff_with_legacy, 7 *_projection_diff); update comment bullets(53-65,80-92); delete tool-name list entries(339,1143); delete mock_cutovermode_rpc(448) + test_zcl_cutovermode/cutoverpreflight_shape_and_dispatch(467,528) + their calls(1673-1674)."
        },
        {
          "file": "lib/test/src/test.c",
          "change": "STRIP registration lines: test_parity_diff_gate(101 ZCL_TEST_ONLY,660), test_reorg_parity(735), test_reorg_projection_parity(736), test_block_log_file(844), test_block_log_legacy(845), test_replay_verify(846), test_mutator(847), test_cutover_tip_parity(848), test_cutover_preflight(849); plus any ZCL_TEST_ONLY subset blocks for shadow_replay_proof/parity_diff. KEEP test_projection_replay_invariant, test_stage_reorg_unwind_parity, test_utxo_projection, test_coins_view_projection, per-projection tests."
        },
        {
          "file": "lib/test/src/test_parallel.c",
          "change": "STRIP X(...) macro rows: reorg_parity(86), reorg_projection_parity(87), conservation_diff_job(120), block_log_file/block_log_legacy/replay_verify(136), mutator/shadow_feeder/shadow_feeder_global(137), shadow_conservation(138), diff_with_legacy_shadow/shadow_replay_proof(139), shadow_replay_full_driver/cutover_tip_parity(140), cutover_flip_dryrun/cutover_postflip_reorg(141), cutover_autorevert(142), cutover_preflight(143), parity_diff_gate(155). KEEP stage_reorg_unwind_parity(87)."
        },
        {
          "file": "lib/test/include/test/test_helpers.h",
          "change": "STRIP extern decls: test_parity_diff_gate(263), test_reorg_parity(329), test_reorg_projection_parity(330), test_cutover_tip_parity(425), test_cutover_preflight(426), test_block_log_file(450), test_block_log_legacy(451), test_replay_verify(452), test_mutator(453), plus shadow_*/diff_with_legacy_shadow/shadow_replay_*/conservation_diff_job/cutover_* decls."
        },
        {
          "file": "lib/storage/src/contacts_projection.c",
          "change": "STRIP: contacts_projection_diff_legacy(325) + private count_legacy_contacts helper. Projection stays; only caller was deleted projection_diff_controller.c."
        },
        {
          "file": "lib/storage/src/onion_announcements_projection.c",
          "change": "STRIP: onion_ann_projection_diff_legacy(281). Projection stays."
        },
        {
          "file": "lib/storage/src/hodl_history_projection.c",
          "change": "STRIP: hodl_history_projection_diff_legacy(273). Projection stays."
        },
        {
          "file": "app/jobs/src/header_admit_stage.c",
          "change": "STRIP duality: inline authoritative_admit unconditionally (drop get_mode==AUTHORITATIVE guard at 279); delete header_admit_set/get_mode(296-310), g_authoritative_hook + set_authoritative_hook, header_admit_stage_diff(539+); drop cutover_modes.h include. KEEP step_admit/log_insert/init/step_once/shutdown."
        },
        {
          "file": "app/jobs/include/jobs/header_admit_stage.h",
          "change": "STRIP: header_admit_mode_t enum(66-68), set/get_mode(72-73), authoritative_hook typedef+setter(107-113), header_admit_stage_diff decl(185) + diff report/sample/enum types. KEEP lifecycle decls."
        },
        {
          "file": "app/jobs/src/validate_headers_stage.c",
          "change": "STRIP duality: always authoritative-mark; delete validate_headers_set/get_mode(487-496), the mode gate, init mode-log(481); drop cutover_modes.h include. KEEP stage body/init/step_once/shutdown."
        },
        {
          "file": "app/jobs/include/jobs/validate_headers_stage.h",
          "change": "STRIP: validate_headers_mode_t enum(99-101), set/get_mode(103-104). KEEP lifecycle decls."
        },
        {
          "file": "app/jobs/src/tip_finalize_stage.c",
          "change": "STRIP duality: make is_authoritative()(485) return true always; delete tip_finalize_set/get_mode(590-604); fix active_chain_authority.is_authoritative wiring(525); update 'shadow mode' init log(550); drop cutover_modes.h include. KEEP step_finalize/set_tip_cb/init/step_once/shutdown/set_authoritative_tip/divergence-counter."
        },
        {
          "file": "app/jobs/include/jobs/tip_finalize_stage.h",
          "change": "STRIP: tip_finalize_mode_t enum(27-29), set/get_mode(34-35). KEEP init/step_once/shutdown/last_height/set_authoritative_tip/divergence decls."
        },
        {
          "file": "lib/validation/src/accept_block_header.c",
          "change": "STRIP: HEADER_ADMIT_MODE enum + header_admit_get_mode extern(153-157), header_admit_get_mode==AUTHORITATIVE branch(334), validate_headers_authoritative_guard(193-362) + its calls(322,404). Header acceptance flows through the single staged path."
        },
        {
          "file": "lib/storage/src/utxo_projection.c",
          "change": "STRIP: g_author static(91), utxo_projection_set_author(93-96), utxo_projection_get_author(98-102). KEEP open/catch_up/seed/reads/commitment/emit_add/emit_spend (single engine)."
        },
        {
          "file": "lib/storage/include/storage/utxo_projection.h",
          "change": "STRIP: utxo_author_t enum(45-48), set_author/get_author decls(50-51), B3 doc comment(34-44)."
        },
        {
          "file": "lib/storage/src/coins_view_stage_backing.c",
          "change": "STRIP: the `if (utxo_projection_get_author()!=UTXO_AUTHOR_STAGE){*out=*legacy;return true;}` LEGACY branch(91-94) so the projection composite is selected unconditionally; convert the NULL-proj fallback(102-117) to a hard LOG_FAIL. KEEP the composite build(111-122). File stays as the projection-backed read adapter."
        },
        {
          "file": "app/jobs/src/utxo_apply_stage.c",
          "change": "STRIP: get_author==STAGE gate at emit_delta(260) -> run on every verified block (if(summary.ok)emit_delta); update stale 'no-op until B7' comment(256-259) + 'shadow mode' log(358). KEEP emit_delta + compute_block_delta."
        },
        {
          "file": "app/jobs/src/utxo_apply_delta.c",
          "change": "STRIP: get_author==STAGE reorg-unwind gate(730) so disconnect/reorg-unwind emits EV_UTXO_SPEND/ADD unconditionally (pairs with deleting the disconnect_block legacy emitters)."
        },
        {
          "file": "lib/validation/src/update_coins.c",
          "change": "STRIP: update_coins_emit_utxo_add_shadow/_spend_shadow(44-88), g_utxo_event_emit_total/fail counters(30-42), call sites in update_coins_with_undo(145 spend,187 add). KEEP update_coins itself (legacy cache mutator) until coins.db retired; only the event-emission half goes."
        },
        {
          "file": "lib/validation/src/connect_block.c",
          "change": "STRIP: disconnect_block's update_coins_emit_utxo_add_shadow(752) + _spend_shadow(799) call sites. Coins-cache mutation stays until the cache is retired."
        },
        {
          "file": "lib/validation/src/connect_tip.c",
          "change": "REWIRE+STRIP: repoint SHA3 checkpoint utxo_commitment_sha3_compute(ndb->db,...)(554) -> utxo_projection_commitment FIRST; then remove durable flush_coins_if_needed(548,718,968), keep coins_view_cache_flush(&view)(523) only as RAM accelerator. Gated on projection seeded+caught-up."
        },
        {
          "file": "lib/validation/src/disconnect_tip.c",
          "change": "STRIP: durable coins.db flush coins_view_cache_flush(&view)(192) + node_db_sync_disconnect_block(208). KEEP in-memory undo-apply + BIP30 invariant check(240-256)."
        },
        {
          "file": "lib/validation/src/process_block_flush_policy.c",
          "change": "STRIP: flush_coins_if_needed coins.db branch coins_view_sqlite_batch_write_ex(235-252); eventually g_coins_sqlite_ptr + set_coins_sqlite_for_commitment(73-84). KEEP the Sapling-tree checkpoint persistence."
        },
        {
          "file": "app/services/src/chain_state_repository.c",
          "change": "STRIP: csr_persist_coins_best_locked(151-203) + persist_coins_best path in csr_commit_tip(686-695); coins_view_cache_set_best_block calls(631,658,710). KEEP csr_validate_locked guards + active_chain_set_tip. tip = tip_finalize progress.kv cursor."
        },
        {
          "file": "app/services/src/chain_tip.c",
          "change": "STRIP: chain_tip_fsync_barrier coins.db cacheflush(57-84) + its call(178). KEEP active_chain_set_tip (RAM tip publication)."
        },
        {
          "file": "app/services/src/utxo_recovery_restore.c",
          "change": "STRIP: utxo_recovery_import_ldb body(52, LOCK-safe copy 100, wipe+coins_view_db_open+node_db_sync_import_utxos 119-129). REWIRE the fast cold-start capability to fast_sync(SHA3 snapshot)->event_log->utxo_projection fold. KEEP utxo_recovery_restore_chain_tip(372)."
        },
        {
          "file": "app/controllers/src/sync_controller_catchup.c",
          "change": "STRIP the import-job half: node_db_sync_import_job_init/_start/_join/_is_started(972-1016) + struct node_db_sync_import_job + worker(968). KEEP node_db_sync_catchup(499), sapling_tree_rebuild(266), node_db_sync_wallet_keys(1122), mempool save/load(1199,1247), catchup_job(900-954). REWIRE node_db_sync_catchup lean-index into block_index_projection fold."
        },
        {
          "file": "app/controllers/src/blockchain_controller_admin.c",
          "change": "REWIRE/DELETE: importutxos RPC(285) node_db_sync_import_utxos -> fast_sync snapshot apply or remove. No operator path may bulk-load legacy utxos from external LevelDB."
        },
        {
          "file": "app/controllers/src/wallet_diagnostic_repair.c",
          "change": "REWIRE: legacy_import(legacy_dir,...)(205) -> wallet_projection fold + on-chain rescan (replaywalletfromchain/rescanblockchain). Drop dead legacy_import.h include(43)."
        },
        {
          "file": "app/controllers/src/snapshot_controller_import.c",
          "change": "REWIRE: legacy_import call(477) -> projection-backed wallet rescan. Drop legacy_import.h include(20). (File already in working-tree-modified set per git status — verify no shadow/cutover decl leaks.)"
        },
        {
          "file": "app/controllers/src/wallet_diagnostic_health.c / wallet_diagnostic_ledger.c / wallet_diagnostic_audit.c / wallet_diagnostic_keys.c",
          "change": "STRIP: the dead `#include \"controllers/legacy_import.h\"` (line 43 in each) — they include but never call legacy_import; remove once legacy_import.h is deleted."
        },
        {
          "file": "config/src/boot.c",
          "change": "STRIP: no_legacy_auto_import blocks at 1892 (zclassicd LevelDB block-index import), 2117 (hard-link ~/.zclassic/blocks), 2292-2309 (utxo_recovery_import_ldb); legacy_import.h include(79); boot_resolve_cold_import_pending_anchor(799-900) unless retargeted. REWIRE to boot_rebuild_from_log() single-engine cold-start (see GAP)."
        },
        {
          "file": "app/controllers/src/blockchain_controller_chain.c",
          "change": "REWIRE: gettxoutsetinfo(335) + balance/listunspent reads off coins_tip(coins.db) -> utxo_projection (get_coins:543, count:603) via coins_view_projection. Read-serving completeness gap."
        },
        {
          "file": "app/controllers/src/blockchain_controller_blocks.c",
          "change": "REWIRE (conditional): getblock block_map_find(206,238) resolves from map_block_index — unchanged if boot rebuilds the map from block_index_projection (GAP stage 8); else rewire to block_index_projection_get. Block bodies via disk_block_io STAY."
        },
        {
          "file": "lib/validation/src/process_block.h / process_block.c",
          "change": "STRIP: g_body_pull fast-sync relax-flag (process_block.c:80, decl process_block.h:70-96) + its readers in connect_tip.c(694,777) once cold-start no longer body-pulls."
        },
        {
          "file": "Makefile",
          "change": "STRIP: BUILD_NODE_TOOL shadow_replay_proof(169). MUTATOR_SRCS(51) auto-empties when mutator/ deleted; remove MUTATOR_SRCS/MUTATOR_INCLUDES(50-51) + their use in ALL_SRCS(58) if left dangling."
        }
      ],
      "baseline_shrink": [
        "tools/scripts/one_result_type_baseline.txt: delete line 23 (app/services/src/cutover_modes.c). KEEP line 24 (legacy_mirror_sync_service.c) — file is KEEP.",
        "tools/scripts/file_size_ceiling_baseline.txt: delete line 13 (legacy_import.c) and line 15 (sync_controller_import.c) — files deleted. Line 14 (sync_controller_catchup.c) and 16 (legacy_mirror_sync_service.c) STAY (files survive).",
        "tools/scripts/one_write_path_baseline.txt: delete line 37 (utxo_projection.h set_author decl) + line 42 (utxo_projection.c set_author def); lines 35-41 (coins_view_sqlite.h/.c batch_write* — file deleted); lines 59,61 (connect_tip/disconnect_tip coins_view_cache_flush durable), lines 64,65 (process_block_flush_policy batch_write_ex/cache_flush); the coins_view_cache_flush admin/repair/boot helper lines 3-29 (blockchain_controller_admin/chain, hodl_controller, repair_controller_utxo, utxo_recovery_service, boot_index, boot_services) once coins.db is not the durable store; lines 30,31 (coins_view.h/.c coins_view_cache_flush) once no durable caller remains. KEEP active_chain_set_tip lines + process_new_block lines (single-engine RAM tip + block-acceptance entrypoint).",
        "tools/scripts/raw_sqlite_allowlist.txt: ALREADY EMPTY (migration-complete, header comments only) — nothing to shrink."
      ],
      "new_code_gaps": [
        {
          "gap": "BLOCK-INDEX + nStatus POPULATION. After the legacy connect path (accept_block.c/process_block_core.c) is deleted, NOTHING populates ms->map_block_index or the BLOCK_HAVE_DATA/BLOCK_VALID_SCRIPTS bits that every stage reads (tip_finalize preconditions_ok:258, utxo_apply:225). header_admit_stage.c + validate_headers_stage.c contain NO block_map_insert/nStatus writes. Either make header_admit own block_index insertion + body_persist set BLOCK_HAVE_DATA, OR rewire all stage reads to block_index_projection (block_index_projection_get_by_height exists at block_index_projection.c:548 with ZERO production readers — the intended-but-unwired seam). This is the central cold-start AND advance blocker.",
          "target_file": "app/jobs/src/header_admit_stage.c"
        },
        {
          "gap": "COLD-START boot_rebuild_from_log(). Replace the deleted legacy auto-import + load_block_index_sqlite (boot.c:1804-2309) with a single-engine cold-start: open event_log unconditionally -> replay block_index_projection to rebuild the block_index map -> replay utxo_projection.catch_up -> seed tip_finalize cursor from progress.kv. Boot must not read a sibling zclassicd datadir. Cold UTXO state comes from fast_sync (SHA3 UTXO snapshot -> event_log seed -> projection fold), not external LevelDB import.",
          "target_file": "config/src/boot.c"
        },
        {
          "gap": "READ-SERVING off the projection. gettxoutsetinfo (line 335) + the wallet UTXO scan behind balance/listunspent currently read coins_tip (coins_view_sqlite over coins.db). After coins.db is deleted these must read utxo_projection via a projection-backed coins_view (coins_view_projection.c exists; utxo_projection_get_coins:543 + utxo_projection_count:603 are byte-compatible). Rewire rpc_blockchain_set_coins_db / coins_tip consumers to the projection.",
          "target_file": "app/controllers/src/blockchain_controller_chain.c"
        },
        {
          "gap": "WALLET-FROM-FOREIGN-DATADIR recovery (if retained). legacy_import.c (mmap-scan of an external node's blk*.dat for wallet outputs/Sapling notes) is deleted; its callers (snapshot_controller_import.c:477, wallet_diagnostic_repair.c:205) need a replacement that scans via the projection-backed block store + on-chain rescan (replaywalletfromchain / rescanblockchain), not a parallel mmap of a foreign node.",
          "target_file": "app/controllers/src/wallet_diagnostic_repair.c"
        },
        {
          "gap": "CONSENSUS-READ repoint BEFORE deleting coins.db. connect_tip.c SHA3 checkpoint (utxo_commitment_sha3_compute(ndb->db,...):554) and coins_view_sqlite.c boot tip-consistency check (280) are the two consensus-critical reads off the legacy utxos table. They must be repointed to utxo_projection_commitment / count BEFORE coins_view_sqlite.c is deleted, or the node loses its integrity gate.",
          "target_file": "lib/validation/src/connect_tip.c"
        }
      ]
    }
  }
}